# 03 Architecture Compat Summary

状态：`compat_summary`

Canonical source：`docs/ARCHITECTURE.md`

本文件仅用于旧的 `docs/gpt/project_sources/` 上传流程兼容，不维护第二份架构事实。若本文与 canonical 文件冲突，以 `docs/ARCHITECTURE.md` 为准。

读取建议：

- 系统分层、API、Web、服务和运行边界，请读取 `docs/ARCHITECTURE.md`。
- 工作站控制平面请同时读取 `docs/workstation/`。

