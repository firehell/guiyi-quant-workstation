# CJ W1 strict no-trade repair: prepared batch

Read-only snapshot refreshed on 2026-09-23; exact machine packet: `prepare.json` (SHA-256 `2b05d5dfcc36b8c0dc7dcbe4d26680303ca69ad3dd6e4a93797a89acdab28c52`). The packet binds the local Canonical root identity, CJ D1/W1 Catalog revision, D1 preimage URIs and byte hashes, old and candidate W1 month URIs and byte hashes, source-code hashes, complete D1 endpoints and row-level OHLC differences. `scripts/cj_weekly_no_trade_repair.py prepare` made **zero provider requests and zero Catalog/Canonical writes**. Independent `inspect` returned `old / old`. The refresh changed only `repair_source_sha256` after the pre-commit target-week assertion was added.

| Contract / month | Complete week | Change | Unchanged |
| --- | --- | --- | --- |
| CJ2305 / 2022-05 | 2022-05-19, 05-20 | 05-20 W1 open 0 → 13720, low 0 → 13320 | high, close, end, trading day, volume, turnover, OI; other month row |
| CJ2309 / 2022-09 | 2022-09-26 through 09-30 | 09-30 W1 low 0 → 12340 | open, high, close, end, trading day, volume, turnover, OI; other two month rows |

The D1 source set is complete under the current Contract lifecycle and Calendar/Session endpoints. Strict no-trade D1 is 2022-05-19 for CJ2305 and 2022-09-29 for CJ2309; the latter has nonzero OI. No D1 is rewritten. Both bad weeks precede the respective rank-1 owner start; warm-up history stays intact. Prior diagnosis inputs are the four read-only files under `/Volumes/扩展盘/guiyi-quant-workstation/outputs/newow-weekly-remaining12-20260923/`, whose hashes were checked in this task.

The read-only impact scan covered all **48 formally open W1 products**: 1,005 physical W1 datasets / 47,990 stored Bars, plus 48 continuous MAIN W1 datasets / 8,515 stored Bars. It found zero stored weeks containing a strict no-trade D1 and zero old/new numerical differences in both scopes. CJ remains outside the formal W1 capability. New MDS quality replay correctly fails closed on the two old CJ weeks until their controlled rebuild; it does not synthesize replacement prices.

An additional **in-memory candidate overlay** replaced only reads of the two target W1 months while leaving the production Catalog and files unchanged. At `2026-09-18T07:00:00.000001Z`, the read-only CJ W1 matrix returned 3/3 main cases READY: trend, oscillation and main-rise each had READY chart, MACD, main-force-control, up/down-energy, zhaoyao-mirror and reference sections; cup-handle was legitimately NOT_APPLICABLE. It used weekly input policy v2, reported no repair targets, no budget exhaustion, zero provider calls and zero writes. The raw report is `candidate-overlay-readiness.json` (SHA-256 `df92c28d095e789659cb6796712b810d63bd16266cee405b87893f52af915d1d`); its audit identity pins the overlay mode, target candidate URIs, the **prior** prepare hash `87f43b1dfc3f898973aa577d884780feb20f8c8c260a539943ae83e07f85f62f`, and unchanged Catalog revision. The refreshed packet differs only in repair-script source hash, so the overlay remains historical value evidence and is not an audit of the refreshed packet. This is a preflight over candidate values, **not** a repaired Catalog revision, natural first load, page acceptance or formal READY claim.

## Controlled execution boundary

The batch scope is exactly the two W1 month partitions above: two immutable candidate files and one Catalog transaction. Provider budget is zero; D1, other products, Scope, notifications and Runtime state are outside this write. Before apply, stop the old writer and establish a consumer version that understands `exchange-daily-no-trade-v2`. The CLI takes the global maintenance lock, recomputes and compares the full frozen packet, publishes candidates, checks physical D1→W1 replay, then commits both pointers. A separate read-only session checks the final active pointers and quality replay. `inspect` classifies both old, both candidate, or mixed/unknown. A failure before commit leaves old pointers; candidate files may remain unreferenced. A commit exception is `COMMIT_OUTCOME_UNKNOWN`: stop, run `inspect`, and do not retry without a fresh safety decision. Old immutable files stay available; pointer restoration would need a separate controlled plan and authorization.

Canonical/Catalog apply, main/tag/release, Runtime promotion and formal CJ opening each require explicit scope-bound authorization. This packet grants none. After an authorized apply, freeze the new revision and run CJ trend, oscillation and main-rise chart, auxiliary, reference and natural first-load acceptance, including owner-prefix warm-up and cross-contract isolation. Legal WARMING/NO_TRADE/non-READY states must remain visible. Any fresh Catalog or source hash drift invalidates this packet and requires a new read-only prepare.
