# 牛哇 D1：SI、J、EG 三品种候选验收

状态：`CANDIDATE_ACCEPTED`；60 品种累计 `57/60`，剩余 B、BZ、PG。仅属固定截止下的只读候选，不是正式 Release、Runtime 或因果收益验收。

## 身份与范围

- 代码：`develop = origin/develop = abd3b4d2a5da9b047ca47521411c60f8d47db49f`。执行期间 develop 从 `c9e0297a0` 前进且改动了共享 MDS 路径，因此旧提交上的页面结果未用于最终验收；数据和页面均在该精确提交重新执行。
- 冻结业务截止：`2026-09-15T07:00:00.000001+00:00`。API `127.0.0.1:8010` 和 Web `127.0.0.1:5174` 的 `/api/preview/identity` 均返回该 SHA、截止与 `local_candidate_readonly`；只读隔离预览在验收后关闭。
- 范围：SI、J、EG × 趋势、震荡、主升浪 × `1d/actual_dominant`，共 9 个首次加载页面。真实浏览器使用 Playwright CLI，每个组合用独立新会话直接导航，等待图表 ready 与历史参考收益摘要完成；不依赖刷新造绿。

## 数据与计算

当前代码分别执行 `PYTHONPATH=services/quant-api:packages/quant-core services/quant-api/.venv/bin/guiyi data newow-readiness --symbol <SI|J|EG> --frequency 1d --matrix --compact --as-of 2026-09-15T07:00:00.000001+00:00 --max-work 10000 --timeout-seconds 300`。3/3 `audited`、`complete=true`、`budget_exhausted=false`；9/9 `main/chart/reference=READY`，修复目标均为 0，provider 请求和写入均为 0。

| 品种 | 依赖结果 | 日线三策略 |
|---|---|---|
| SI | 45 `DATA_READY`，1 `DATA_INTERRUPTED:PRICE_UNAVAILABLE` | 3/3 READY |
| J | 17 `DATA_READY`，2 `DATA_INTERRUPTED:PRICE_UNAVAILABLE` | 3/3 READY |
| EG | 17 `DATA_READY`，2 `DATA_INTERRUPTED:PRICE_UNAVAILABLE` | 3/3 READY |

## 首次加载页面与历史参考收益

9/9 页面 `data-chart-state=ready`，参考卡显示“所选统计区间已按权威截止完成计算”和“收益合计”。数值是页面参考交易的零手续费、零滑点、简单相加百分点，不是账户成交或因果研究收益。

| 品种 | 策略 | 已完成参考交易 | 收益合计 | 历史覆盖提示 |
|---|---|---:|---:|---|
| SI | 趋势 | 64 | +55.91 百分点 | 无局部覆盖提示 |
| SI | 震荡 | 16 | +38.38 百分点 | 历史覆盖不完整；仅统计有效区段 |
| SI | 主升浪 | 7 | -2.35 百分点 | 无局部覆盖提示 |
| J | 趋势 | 72 | +130.68 百分点 | 无局部覆盖提示 |
| J | 震荡 | 23 | +67.83 百分点 | 无局部覆盖提示 |
| J | 主升浪 | 10 | +11.69 百分点 | 无局部覆盖提示 |
| EG | 趋势 | 71 | +122.34 百分点 | 无局部覆盖提示 |
| EG | 震荡 | 24 | +39.97 百分点 | 无局部覆盖提示 |
| EG | 主升浪 | 6 | +1.27 百分点 | 无局部覆盖提示 |

精确 `abd3b4d2a` 上的 9 个独立浏览器首次加载会话均为 console 0 error；逐页记录到的业务请求没有非 HTTP 200 响应。旧 `c9e0297a0` 探索会话曾在开发服务器 HMR 时出现取消请求和一次 HTTP 429，未计入本轮结果。复验结束后预览已关闭。

## 剩余三品种与边界

同一截止和代码下只读复核：B 剩 1 个、BZ 剩 2 个、PG 剩 17 个 `PROPOSED` 数据修复目标，三者三策略 `main/chart/reference` 均为 `DATA_UNAVAILABLE`。PG 的既有回执不完整问题见 `outputs/newow-pg-d1-20260917/preflight.md`，不能自动重试。三者未进入页面验收。

本次未请求 RQData、未写 Canonical/Catalog、未修改正式服务、Release 或 Runtime。原 43 品种和后续 10 品种、EB 的验收分别保留在 `outputs/newow-d1-43-release-gate-20260916/验收报告.md`、`outputs/newow-d1-10-acceptance-20260917/验收报告.md`、`outputs/newow-eb-d1-20260917/preflight.md`，不能将不同候选提交写成一次全 57 品种 exact-commit 浏览器复验。
