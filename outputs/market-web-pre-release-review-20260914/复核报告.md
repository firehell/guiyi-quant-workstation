# Market Web 十一项修改复核

日期：2026-09-14。状态：PARTIAL。结论：阻塞本轮发布，先修正并补验收。

本次只读审查 develop `8dd2c814e`，以原计划基线 `b823fc9cc4b6c6182e753a70f60f4b29e6f90d8c` 比较相关改动，不评价同期周线恢复任务。没有修改产品代码、生产数据或服务。复核期间出现其他任务的 `.codex/config.toml` 修改，未读取内容或改动。

## Standards 独立审查

1. **苏冰状态误报**：`apps/quant-web/src/utils/subingDetailViewModel.ts:55-61` 忽略权威 `runtime.status`，只检查 Rule 错误。直接调用当前代码，disabled/degraded 且 Rule 无错误、有历史评价时，均显示“全局正常 · 最近已评估”。应先显示 Runtime 的停止、降级或正常状态，再独立表达 Rule 评价。
2. **密集标签丢失及极窄区域越界**：`apps/quant-web/src/utils/referenceCalloutLayout.ts:51-59` 完整框和紧凑框都放不下时直接跳过。主审直接调用当前模块：390×220 内 100 个同锚点可见输入只返回 48 个布局节点；25×25 单点返回 left=-5、top=-5、28×28。丢失的是可访问展示节点，不是底层 Marker 或配对结果。需有能访问全部身份的溢出入口，并处理小于节点尺寸的视口。
3. **选择器再次点击缺少打开路径**：`apps/quant-web/src/components/market/ProductSelector.vue:43-59,99-105` 关闭后恢复输入焦点并抑制重开，但输入只监听 focus/input，未监听再次点击。输入已聚焦时点击不产生 focus，列表不会打开。此项为源码确认的交互缺口，本轮未完成真实组件浏览器复现。
4. **候选预览合同未同步**：`services/quant-api/app/preview.py:30-36` 新允许 SuBing reference，但 `openspec/specs/newow-product-reference-trading/spec.md:31-33` 的仅允许路由清单仍未包含它；预览 identity 的 cutoff_scope 仍为 bars_and_newow。需同步路由及截止范围合同。未发现该路由实际写入。

## Spec 独立审查

- 第5项仍有异常 Runtime 被解释为正常的问题。
- 第11项仍违反密集模式保留身份和操作入口、边界不越界的要求。
- 第3项性能证据不足：计划要求新候选进程/连接池的冷启动、连续三次和两个并发请求。现报告使用已编译进程中的新浏览器与刷新计时；真实 E2E 仅记录计时，没有小于10秒的断言。不能把这些证据视为完整性能 Gate。
- 第9/11项实际验收覆盖不足：完整统计区间操作主要验证 AU trend 的输入值，未覆盖 AU/JM 三策略的响应和最终统计状态；真实标签只检查 AU trend，缺苏冰及另外两策略、1920宽度，且全屏后没有重新检查盒体、碰撞与身份数量。
- 周线截止来自后端已验证 W1 replay，未发现前端自行猜周五的实现问题。

## 十一项当前判断

| 项 | 判断 |
|---|---|
| 1 焦煤苏冰历史参考 | 独立诊断实现已存在；不可笼统宣称全部恢复或全部不可用，见本次真实 API 回读。长窗口仍需独立数据定位。 |
| 2 大写 AU/JM | 相关实现及定向测试未发现新增问题；本轮未对候选做真实大小写 HTTP 对照。 |
| 3 首页性能 | 部分完成；完整冷启动、三次刷新、双并发及恢复验收证据需补齐。 |
| 4 选择器焦点 | 原重开问题有修复，但再次鼠标点击已聚焦输入框的路径仍缺失。 |
| 5 苏冰状态 | 未完成，存在 Runtime 停止/降级误报正常。 |
| 6 数字精度 | 相关格式化实现与本轮定向测试通过，未发现新增问题。 |
| 7 时间与术语 | 已有统一格式化，但苏冰主展示仍含 Runtime、Rule、warm-up、Bar 等词；需按“中文主展示、技术身份详情”再扫一遍。 |
| 8 五日涨跌 | 无来源的占位项已移除，本轮未发现新增问题。 |
| 9 周线 partial | 已有解释和操作；完整真实矩阵验收不足。 |
| 10 状态文档 | STATUS 已记录 v1.10.10；候选预览 canonical/identity 仍有漂移，验收报告“全部通过”结论需更正。 |
| 11 图表标签 | 未完成，密集节点会丢失，极窄区域越界，真实全屏/多策略验收不足。 |

## 本次实际验证

前端：61 passed，0 failed。

```sh
pnpm_config_verify_deps_before_run=false pnpm -C apps/quant-web exec node --test tests/marketDisplay.test.ts tests/productSelector.test.ts tests/referenceCalloutLayout.test.ts tests/useSubingAlertFacts.test.ts tests/subingReference.test.ts tests/newowReferencePanel.test.ts tests/marketHomeResource.test.ts tests/marketDetailViewModel.test.ts
```

后端：98 passed，3.03秒。

```sh
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=services/quant-api:packages/quant-core services/quant-api/.venv/bin/python -m pytest -q -p no:cacheprovider services/quant-api/tests/test_subing_reference_api.py services/quant-api/tests/test_subing_reference_service.py services/quant-api/tests/newow/test_market_newow_product_api.py services/quant-api/tests/newow/test_candidate_preview.py
```

本地正式 API 只读回读（不代表候选验收）：

- GET /health：200，version=1.10.10，readonly=true。
- JM SuBing reference，固定 as_of=2026-09-14T11:00:00Z，未指定 since/through：200；实际统计区间为2026-08-18至2026-09-14，18笔已关闭、1笔开放、1笔换月中断。
- AU/JM 分别指定 since=2025-09-15、through=2026-09-14、同一 as_of：均409，SUBING_REFERENCE_DATA_UNAVAILABLE。正式版响应未提供本次可核对的详细缺口原因，因此不把旧报告中的 JM2609 精确缺口数字作为本次新确认事实。

现有测试全部通过仍未覆盖上述反例。本轮未重跑全量 build、真实浏览器矩阵或性能测试；候选8010/5174未在监听，计算机浏览器连接超时，不把工具连接失败判断为产品故障。旧报告的浏览器结果仅作为历史证据。

## 下一步

把状态映射、标签溢出、选择器再次点击及合同同步作为一批修正，再按原计划补齐真实候选性能和图表验收，并更新验收结论。此复核不授权数据 apply、main/tag/release 或 Runtime promotion。
