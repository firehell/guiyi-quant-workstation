# 06 Web Compat Summary

状态：`compat_summary`

Canonical source：`docs/ARCHITECTURE.md`; `apps/quant-web/src/app/router.ts`

本文件仅用于旧的 `docs/gpt/project_sources/` 上传流程兼容，不维护第二份 Web 事实。若本文与 canonical 文件冲突，以 canonical source 为准。

读取建议：

- Web 架构、页面边界和 API 关系，请读取 `docs/ARCHITECTURE.md`。
- 前端路由事实请读取 `apps/quant-web/src/app/router.ts`。

