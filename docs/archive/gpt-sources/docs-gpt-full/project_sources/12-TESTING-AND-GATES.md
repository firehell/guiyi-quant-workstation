# 12 Testing And Gates Compat Summary

状态：`compat_summary`

Canonical source：`TESTING.md`

本文件仅用于旧的 `docs/gpt/project_sources/` 上传流程兼容，不维护第二份测试事实。若本文与 canonical 文件冲突，以 `TESTING.md` 为准。

读取建议：

- 后端、前端、数据、回测、文档和 Gate 验证命令，请读取 `TESTING.md`。
- 未运行测试不能写成已通过；环境缺失、依赖缺失和 Gate 阻塞要如实记录。

