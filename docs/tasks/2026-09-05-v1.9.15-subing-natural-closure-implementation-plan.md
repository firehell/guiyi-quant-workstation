# v1.9.15 Release 与 SuBing 自然推送闭环 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将执行时最新、已收敛的 full `develop` 全量冻结为 `v1.9.15` Release Candidate，完成 exact-head 全量验证和双轴 Review；随后在独立人工 Gate 下完成 release、PF2611 physical-contract warm-up、五项 Runtime promotion，并等待自然 SuBing 15m Event、PushPlus provider acceptance 与 Owner 微信实际送达确认。

**Architecture:** 本计划不新增策略、账户或交易子系统。现有链路保持 `RQData -> Canonical -> MarketDataService -> single Alert Runtime -> immutable AlertEvent -> one-shot PushPlus`；full `develop` 只作为 execution-time frozen exact snapshot 进入 `v1.9.15`，不自动扩大 Newow/交易 Runtime 权限。PF2611 `through` 由 exact-tag 执行时的 `DatabaseCoverageSource.latest_complete_day(("pf",))` 只读解析；若晚于 `2026-09-03` 直接使用，不增加日期确认 Gate。

**Tech Stack:** Git/GitHub PR + annotated tag + GitHub Release、Python 3.13、FastAPI、SQLAlchemy/PostgreSQL、RQData、Canonical Parquet、Redis/launchd、Vue 3/TypeScript、pytest、Ruff、Mypy、Node test、Playwright、OpenSpec、PushPlus。

**Spec:** `docs/tasks/2026-09-05-v1.9.15-subing-natural-closure-design.md`

## Global Constraints

- 任务开始时先 fresh 读取 `STATUS.md`、`AGENTS.md`、`docs/DEVELOPMENT.md`、`PROJECT_SOURCE.md`、`DECISIONS.md`、`docs/ARCHITECTURE.md`、`TESTING.md`、本 Spec 与本 Plan。
- 当前 production Release/Runtime 以 fresh `STATUS.md` 为准；设计时事实为 `v1.9.14@ca15456eaff988db4fe61c37657ca37302a7f977`。
- `v1.9.15` release source 是 **Task 2 开始时 fresh `origin/develop` 的完整 tracked tree**，不是旧 `codex/release-v1.9.15@2eb33e6d...`。
- Task 2 一旦冻结 `$RC_SOURCE_DEVELOP_SHA` 并开始 exact-head 验证，后续 `develop` 前进不自动进入该 RC；如发现 release blocker，修复必须先进入 `develop`，再从 Task 2 重新冻结。
- `codex/release-v1.9.15` 只允许安全 fast-forward；禁止 selective cherry-pick、squash、force update、release-only hotfix。
- full develop 进入 release 不等于扩大生产授权：不得新增/启用 Newow Alert、Newow Runtime、参考交易、风险模块、模拟账户、PostgreSQL 交易 Ledger、订单或真实账户能力。
- SuBing 固定为 `subing_ths_alert_15m_v1` / `subing_ths_15m_v3` / completed `actual_dominant` 15m / MACD exact CROSS + EMA21 / same physical-contract warm-up；不得增加隐藏过滤。
- `auto_order=false` 保持不变。
- 设计基线 SuBing Scope 为 operational 60 × 15m，`scope_hash=ce1daca77aeb1abe134806b67aebd96b2c35db3ba82aa10af58f6e5a2e4f5fa2`；本计划不重新 activation、不修改 Rule/Scope/audience/Topic/token。
- Release、真实 RQData/Canonical apply、Runtime promotion 是三个独立真实 mutation Gate；失败后的真实重试也是新的 Gate。Task 6、Task 9、Task 10 在首次真实 mutation 前必须取得本轮明确执行意图。
- PF2611 `$THROUGH_DATE` 自动解析为权威最近完整交易日；`> 2026-09-03` 直接继续，`< 2026-09-03` 或权威冲突则 fail-closed。
- warm-up apply 只处理 `pf/PF2611` physical contract family，只接受真实 RQData；禁止 continuous、上一主力、跨合约、Live-only 或 synthetic seed。
- synthetic Candidate、replay、backfill、手工 AlertEvent、手工 PushPlus、手工 after-market 都不能替代自然验收。
- Event 必须先持久化，再最多一次 PushPlus transport；provider accepted 不等于微信实际送达。
- 不读取、打印、提交或记录任何 token、密码、cookie、license、私钥或 Git 外通知配置内容。
- 任一必要检查失败即停止当前 Gate；不得通过 fallback、override、手工补证或自动 retry 推进状态。

## File / Authority Map

**计划内主动修改：**

- `STATUS.md`：只同步已发生 release/Runtime/natural evidence 和 pending Gate。
- `docs/tasks/2026-09-05-v1.9.15-subing-natural-closure-design.md`：本任务已批准的设计事实源。
- `docs/tasks/2026-09-05-v1.9.15-subing-natural-closure-implementation-plan.md`：本计划。
- PR `#333` metadata/comments：RC exact identity、验证、Review、release Gate。
- Issue `#307`：SuBing 自然闭环进度与最终关闭。

**默认只读，不计划主动修改 production source：**

- `services/quant-api/app/version.py`
- `services/quant-api/pyproject.toml`
- `services/quant-api/app/market_data/coverage_source.py`
- `services/quant-api/app/guiyi_cli/data_parser.py`
- `services/quant-api/app/guiyi_cli/data_commands.py`
- `services/quant-api/app/market_data/historical_data_manager.py`
- `services/quant-api/app/market_data/market_read_service.py`
- `services/quant-api/app/alerts/subing_scope_activation.py`
- `services/quant-api/app/alerts/models.py`
- `scripts/ops/macos/install-local-services.sh`
- `scripts/ops/macos/run-local-service.sh`
- `scripts/ops/macos/local-services-status.sh`
- `openspec/specs/subing-ths-alert/spec.md`
- `PROJECT_SOURCE.md`、`DECISIONS.md`、`docs/ARCHITECTURE.md`

若 exact RC 验证证明上述只读文件存在 release blocker，必须按 Task 5 blocker 路径先修回 `develop`，不能直接修改 release branch。

---

### Task 0: 将已批准 Design + Plan 纳入 `develop`

**Files:**
- Source branch: `docs/v1.9.15-subing-natural-closure`
- Add to develop: `docs/tasks/2026-09-05-v1.9.15-subing-natural-closure-design.md`
- Add to develop: `docs/tasks/2026-09-05-v1.9.15-subing-natural-closure-implementation-plan.md`

**Interfaces:**
- Produces: `develop` 内可追溯的 Design + Plan。

- [ ] **Step 1: fresh 读取 branch identity**

```bash
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"
git fetch origin develop docs/v1.9.15-subing-natural-closure
git status --short --branch
git rev-parse origin/develop
git rev-parse origin/docs/v1.9.15-subing-natural-closure
```

Expected: 无未知 dirty path。

- [ ] **Step 2: 检查 docs branch 范围**

```bash
git diff --name-status \
  origin/develop...origin/docs/v1.9.15-subing-natural-closure
```

Expected: 只有本阶段 Design/Plan；出现其它 source/config 修改即停止。

- [ ] **Step 3: 文档验证**

```bash
openspec validate --specs --strict --no-interactive
python3 scripts/engineering/secret_scan.py --json
git diff origin/develop...origin/docs/v1.9.15-subing-natural-closure --check
```

Expected: OpenSpec `0 failed`、secret `finding_count=0`、diff check exit 0。

- [ ] **Step 4: 创建或复用 docs PR 并合入 develop**

```bash
DOCS_PR="$(gh pr list \
  --head docs/v1.9.15-subing-natural-closure \
  --base develop \
  --state open \
  --json number \
  --jq '.[0].number // empty')"
if [ -z "$DOCS_PR" ]; then
  DOCS_PR_URL="$(gh pr create \
    --base develop \
    --head docs/v1.9.15-subing-natural-closure \
    --title 'docs: define v1.9.15 SuBing natural closure' \
    --body 'Adds the approved v1.9.15 full-develop release/SuBing natural-closure design and implementation plan. No production mutation.')"
  DOCS_PR="${DOCS_PR_URL##*/}"
fi
gh pr merge "$DOCS_PR" --merge
```

- [ ] **Step 5: fresh readback**

```bash
git fetch origin develop
git show origin/develop:docs/tasks/2026-09-05-v1.9.15-subing-natural-closure-design.md >/dev/null
git show origin/develop:docs/tasks/2026-09-05-v1.9.15-subing-natural-closure-implementation-plan.md >/dev/null
```

Expected: 两文件均存在于 fresh `origin/develop`。

---

### Task 1: 在 `develop` 同步 full-develop RC 策略当前事实

**Files:**
- Modify: `STATUS.md`
- Test: `tests/engineering/test_repository_hygiene.py`
- Test: `tests/engineering/test_canonical_consistency.py`

**Interfaces:**
- Produces: 不预写未来成功的最新 release-candidate 状态。

- [ ] **Step 1: 建立独立 status worktree**

```bash
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"
git fetch origin develop
test ! -e /private/tmp/guiyi-v1.9.15-status
git worktree add -b chore/v1.9.15-full-develop-status \
  /private/tmp/guiyi-v1.9.15-status origin/develop
cd /private/tmp/guiyi-v1.9.15-status
git status --short --branch
```

- [ ] **Step 2: 修改 `STATUS.md` 的 `v1.9.15 Release candidate` 行**

替换为以下当前事实，不写尚未产生的 exact RC SHA：

```markdown
| `v1.9.15` Release candidate | Owner 已决定下一候选必须全量采用 Gate A 开始时 execution-time latest clean `develop` 的完整 tracked tree。PR `#333` 现有旧 head/旧验证仍为 stale，直到 release branch 被安全 fast-forward 到冻结的 full-develop snapshot，并对新的 exact head 完成 fresh 全量验证与 Standards/Spec 双轴 Review。此状态不授权 `main`、tag、GitHub Release、RQData/Canonical apply、Runtime promotion 或真实通知。 |
```

保留 production `v1.9.14`、2026-09-03 `LIVE_DOMINANT_MISMATCH`、Scope、Event=0 等当前事实。

- [ ] **Step 3: 验证**

```bash
PYTHONPATH=services/quant-api:packages/quant-core \
  uv run --project services/quant-api pytest -q \
  tests/engineering/test_repository_hygiene.py \
  tests/engineering/test_canonical_consistency.py
openspec validate --specs --strict --no-interactive
python3 scripts/engineering/secret_scan.py --json
git diff --check
```

Expected: all exit 0。

- [ ] **Step 4: commit/push/merge 到 develop**

```bash
git add STATUS.md
git commit -m "docs(status): select full develop for v1.9.15 rc"
git push -u origin chore/v1.9.15-full-develop-status
STATUS_PR_URL="$(gh pr create \
  --base develop \
  --head chore/v1.9.15-full-develop-status \
  --title 'docs(status): select full develop for v1.9.15 rc' \
  --body 'Records the approved full-develop RC source policy only. No release or production mutation.')"
STATUS_PR="${STATUS_PR_URL##*/}"
gh pr merge "$STATUS_PR" --merge
```

- [ ] **Step 5: fresh readback**

```bash
cd "$REPO_ROOT"
git fetch origin develop
git show origin/develop:STATUS.md | grep -F 'execution-time latest clean `develop`'
```

---

### Task 2: 冻结 latest full `develop` 并刷新 `codex/release-v1.9.15`

**Files:**
- No production file changes.
- Git refs: `origin/develop`, `origin/codex/release-v1.9.15`
- GitHub: PR `#333`

**Interfaces:**
- Produces: `$RC_SOURCE_DEVELOP_SHA`, `$RC_SOURCE_TREE_SHA`, `$RC_HEAD_SHA`, `$RC_TREE_SHA`。

- [ ] **Step 1: fresh fetch/freeze**

```bash
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"
git fetch origin develop main codex/release-v1.9.15
RC_SOURCE_DEVELOP_SHA="$(git rev-parse origin/develop)"
RC_SOURCE_TREE_SHA="$(git rev-parse "${RC_SOURCE_DEVELOP_SHA}^{tree}")"
OLD_RC_HEAD="$(git rev-parse origin/codex/release-v1.9.15)"
printf 'RC_SOURCE_DEVELOP_SHA=%s\nRC_SOURCE_TREE_SHA=%s\nOLD_RC_HEAD=%s\n' \
  "$RC_SOURCE_DEVELOP_SHA" "$RC_SOURCE_TREE_SHA" "$OLD_RC_HEAD"
```

- [ ] **Step 2: 证明可安全 fast-forward**

```bash
git merge-base --is-ancestor "$OLD_RC_HEAD" "$RC_SOURCE_DEVELOP_SHA"
```

Expected: exit 0；否则停止，禁止 force。

- [ ] **Step 3: 非强制更新 release branch**

```bash
git push origin \
  "$RC_SOURCE_DEVELOP_SHA:refs/heads/codex/release-v1.9.15"
git fetch origin codex/release-v1.9.15
RC_HEAD_SHA="$(git rev-parse origin/codex/release-v1.9.15)"
RC_TREE_SHA="$(git rev-parse "${RC_HEAD_SHA}^{tree}")"
test "$RC_HEAD_SHA" = "$RC_SOURCE_DEVELOP_SHA"
test "$RC_TREE_SHA" = "$RC_SOURCE_TREE_SHA"
```

- [ ] **Step 4: 创建 exact detached validation worktree**

```bash
test ! -e /private/tmp/guiyi-v1.9.15-rc
git worktree add --detach /private/tmp/guiyi-v1.9.15-rc "$RC_HEAD_SHA"
cd /private/tmp/guiyi-v1.9.15-rc
git status --short --branch
test "$(git rev-parse HEAD)" = "$RC_HEAD_SHA"
```

- [ ] **Step 5: 更新 PR #333 metadata**

PR body/comment 记录 `$RC_SOURCE_DEVELOP_SHA`、`$RC_SOURCE_TREE_SHA`、`$RC_HEAD_SHA`、`$RC_TREE_SHA`，明确旧 `2eb33e6d...` 测试/Review stale、Gate A 尚未完成、无外部 mutation 授权。只改 GitHub metadata，不改 RC tree。

- [ ] **Step 6: 校验 release version identity**

```bash
PYTHONPATH=services/quant-api:packages/quant-core \
  uv run --project services/quant-api python - <<'PY'
import tomllib
from pathlib import Path
from app.version import APP_VERSION
assert APP_VERSION == "1.9.15", APP_VERSION
with Path("services/quant-api/pyproject.toml").open("rb") as fh:
    data = tomllib.load(fh)
assert data["project"]["version"] == "1.9.15", data["project"]["version"]
print("VERSION_IDENTITY_OK=1.9.15")
PY
```

---

### Task 3: exact RC Backend / SuBing / Newow 全量验证

**Files:**
- Test/read only: `services/quant-api/**`, `packages/quant-core/**`, `tests/engineering/**`

**Interfaces:**
- Consumes: Task 2 exact `$RC_HEAD_SHA`。
- Produces: fresh backend/Newow/engineering/static evidence。

- [ ] **Step 1: 锁定依赖**

```bash
cd /private/tmp/guiyi-v1.9.15-rc
uv sync --project services/quant-api --locked
```

- [ ] **Step 2: Backend full**

```bash
PYTHONPATH=services/quant-api:packages/quant-core \
  uv run --project services/quant-api pytest -q \
  -m "not isolated_postgresql and not manual_acceptance" \
  services/quant-api/tests
```

Expected: exit 0；记录 fresh counts，不复用 convergence 历史数字。

- [ ] **Step 3: SuBing/MarketRead/Alert/Scope 定向**

```bash
PYTHONPATH=services/quant-api:packages/quant-core \
  uv run --project services/quant-api pytest -q \
  services/quant-api/tests/test_subing_ths_kernel.py \
  services/quant-api/tests/test_market_read_service.py \
  services/quant-api/tests/test_alert_registry.py \
  services/quant-api/tests/test_alert_evaluator.py \
  services/quant-api/tests/test_alert_service.py \
  services/quant-api/tests/test_alert_notification.py \
  services/quant-api/tests/test_alert_notification_config.py \
  services/quant-api/tests/test_alert_pushplus.py \
  services/quant-api/tests/test_alert_runtime.py \
  services/quant-api/tests/test_runtime_health.py \
  services/quant-api/tests/test_alert_api.py \
  services/quant-api/tests/test_alert_cli.py \
  services/quant-api/tests/test_subing_scope_activation.py
```

- [ ] **Step 4: physical-contract warm-up 定向**

```bash
PYTHONPATH=services/quant-api:packages/quant-core \
  uv run --project services/quant-api pytest -q \
  services/quant-api/tests/test_market_read_service.py \
  services/quant-api/tests/data_foundation/test_catalog_and_service.py \
  services/quant-api/tests/data_foundation/test_historical_data_manager.py \
  services/quant-api/tests/data_foundation/test_cli.py \
  services/quant-api/tests/test_market_home_projection_invalidation.py
```

- [ ] **Step 5: Newow fresh regression**

```bash
PYTHONPATH=services/quant-api:packages/quant-core \
  uv run --project services/quant-api pytest -q \
  -m "not isolated_postgresql and not manual_acceptance" \
  services/quant-api/tests/newow
```

- [ ] **Step 6: engineering + static**

```bash
PYTHONPATH=services/quant-api:packages/quant-core \
  uv run --project services/quant-api pytest -q tests/engineering
uv run --project services/quant-api python -m ruff check \
  services/quant-api/app services/quant-api/tests packages/quant-core/guiyi_quant tests/engineering
PYTHONPATH=services/quant-api:packages/quant-core MYPYPATH=services/quant-api:packages/quant-core \
  uv run --project services/quant-api mypy --explicit-package-bases --ignore-missing-imports \
  services/quant-api/app packages/quant-core/guiyi_quant
```

- [ ] **Step 7: exact/clean guard**

```bash
git status --short
test "$(git rev-parse HEAD)" = "$RC_HEAD_SHA"
```

All commands must exit 0 before Task 4。

---

### Task 4: exact RC Web / E2E / OpenSpec / Security 验证与 active-surface guard

**Files:**
- Test/read only: `apps/quant-web/**`, `openspec/specs/**`, `deploy/**`, engineering guards。

**Interfaces:**
- Produces: fresh Web/E2E/security evidence + “full develop 未自动扩权”证据。

- [ ] **Step 1: 锁定 Web 依赖**

```bash
pnpm --dir apps/quant-web install --frozen-lockfile
```

- [ ] **Step 2: ownership + unit + build**

```bash
pnpm --dir apps/quant-web run check:alert-rules
pnpm --dir apps/quant-web test
pnpm --dir apps/quant-web build
```

- [ ] **Step 3: full Playwright E2E**

```bash
pnpm --dir apps/quant-web test:e2e
```

- [ ] **Step 4: OpenSpec/security/diff**

```bash
openspec validate --specs --strict --no-interactive
python3 scripts/engineering/secret_scan.py --json
git diff --check
```

Expected: OpenSpec `0 failed`、secret `finding_count=0`、其它 exit 0。

- [ ] **Step 5: active surface/launchd guard**

```bash
PYTHONPATH=services/quant-api:packages/quant-core \
  uv run --project services/quant-api pytest -q \
  tests/engineering/test_repository_hygiene.py \
  tests/engineering/test_canonical_consistency.py \
  tests/engineering/test_alert_runtime_launchd.py \
  tests/engineering/test_market_runtime_launchd.py
```

Expected: 仅既有 Market/Alert Runtime authority；full develop 不自动启用 Newow/订单面。

- [ ] **Step 6: migration applicability**

```bash
git diff --name-only v1.9.14...HEAD -- services/quant-api/alembic services/quant-api/migrations
```

若无输出：记录 `ISOLATED_POSTGRESQL=NOT_APPLICABLE`。若有 migration 文件：只按 `TESTING.md` 使用专用空白可销毁数据库运行 isolated suite；严禁 production DB。

---

### Task 5: 同一 exact RC 双轴独立 Review + GitHub checks readback

**Files:**
- No tree changes unless blocker。
- GitHub: PR `#333` comments/reviews。

**Interfaces:**
- Produces: `FULL_DEVELOP_RC_APPROVED_FOR_RELEASE_GATE` 或 blocker。

- [ ] **Step 1: Review 前再次冻结 remote identity**

```bash
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"
git fetch origin codex/release-v1.9.15
REMOTE_RC_HEAD="$(git rev-parse origin/codex/release-v1.9.15)"
test "$REMOTE_RC_HEAD" = "$RC_HEAD_SHA"
test "$(git rev-parse "${REMOTE_RC_HEAD}^{tree}")" = "$RC_TREE_SHA"
```

- [ ] **Step 2: Standards Review**

派发 fresh reviewer，只审 `$RC_HEAD_SHA`；输出 P1/P2/P3。通过条件：`P1=0 / P2=0`，P3 明确非阻断。

- [ ] **Step 3: Spec Review**

派发另一个 fresh reviewer，独立读取 Design、OpenSpec、AGENTS/STATUS/PROJECT_SOURCE/DECISIONS，对同一 `$RC_HEAD_SHA` 审查；通过条件同上。

- [ ] **Step 4: GitHub checks readback**

```bash
gh pr checks 333 || true
gh api repos/firehell/guiyi-quant-workstation/commits/$RC_HEAD_SHA/status
```

无 checks/status 时记录 `NO_CHECKS_REPORTED`，不能写 `CHECKS_PASSED`。

- [ ] **Step 5: PR evidence**

在 PR #333 comment 记录 exact head/tree、Task 3/4 fresh 结果、两轴 Review、GitHub checks 状态和 `RELEASE_GATE_PENDING`；不修改 RC tree。

- [ ] **Step 6: blocker path**

任一必要测试失败或 Review 出现 P1/P2：停止 release；从 fresh `origin/develop` 创建独立 fix worktree，先写 RED test，再最小修复并普通 PR 合入 develop；随后回到 **Task 2**，旧 RC 全部证据作废。

通过状态：`FULL_DEVELOP_RC_APPROVED_FOR_RELEASE_GATE`。

---

### Task 6: Gate B — 发布 `v1.9.15`

**Files:**
- GitHub PR `#333`
- Annotated tag `v1.9.15`
- GitHub Release `v1.9.15`

**Interfaces:**
- Produces: `$MAIN_SHA`, `$RELEASE_COMMIT_SHA`, `$RELEASE_TREE_SHA`。

- [ ] **Step 1: fresh 解析 release Gate identity**

```bash
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"
git fetch origin main codex/release-v1.9.15
MAIN_SHA="$(git rev-parse origin/main)"
test "$(git rev-parse origin/codex/release-v1.9.15)" = "$RC_HEAD_SHA"
git merge-base --is-ancestor "$MAIN_SHA" "$RC_HEAD_SHA"
printf 'version=v1.9.15\nrc_source_develop_sha=%s\nrc_head_sha=%s\nrc_tree_sha=%s\nmain_sha=%s\npr=333\n' \
  "$RC_SOURCE_DEVELOP_SHA" "$RC_HEAD_SHA" "$RC_TREE_SHA" "$MAIN_SHA"
```

- [ ] **Step 2: 停止并取得 Owner release mutation 授权**

授权对象就是 Step 1 打印的 exact identity。没有本轮明确授权，不执行 Step 3。

- [ ] **Step 3: mutation 前确认 tag/Release 不存在**

```bash
if git ls-remote --exit-code --tags origin refs/tags/v1.9.15 >/dev/null 2>&1; then
  printf 'v1.9.15 tag already exists\n' >&2
  exit 1
fi
if gh release view v1.9.15 >/dev/null 2>&1; then
  printf 'v1.9.15 GitHub Release already exists\n' >&2
  exit 1
fi
```

- [ ] **Step 4: 合并 PR #333**

```bash
gh pr merge 333 --merge
git fetch origin main
RELEASE_COMMIT_SHA="$(git rev-parse origin/main)"
RELEASE_TREE_SHA="$(git rev-parse "${RELEASE_COMMIT_SHA}^{tree}")"
test "$RELEASE_TREE_SHA" = "$RC_TREE_SHA"
```

Tree 不一致则停止，不创建 tag/Release。

- [ ] **Step 5: annotated tag + GitHub Release**

```bash
git tag -a v1.9.15 "$RELEASE_COMMIT_SHA" -m "v1.9.15"
git push origin refs/tags/v1.9.15
gh release create v1.9.15 \
  --target "$RELEASE_COMMIT_SHA" \
  --title "v1.9.15" \
  --generate-notes
```

- [ ] **Step 6: exact readback**

```bash
git fetch origin main --tags
test "$(git rev-parse 'v1.9.15^{}')" = "$RELEASE_COMMIT_SHA"
test "$(git rev-parse 'v1.9.15^{tree}')" = "$RC_TREE_SHA"
gh release view v1.9.15 --json tagName,targetCommitish,name,isDraft,isPrerelease,url
```

通过状态：`RELEASED_V1_9_15`。仍不是 Runtime promoted/ready。

---

### Task 7: 将 release 事实写回 `develop`

**Files:**
- Modify: `STATUS.md`

**Interfaces:**
- Produces: develop 上准确的 `RELEASED / warm-up pending / Runtime pending` 状态。

- [ ] **Step 1: fresh develop docs worktree**

```bash
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"
git fetch origin develop
test ! -e /private/tmp/guiyi-v1.9.15-released-status
git worktree add -b docs/v1.9.15-released-status \
  /private/tmp/guiyi-v1.9.15-released-status origin/develop
cd /private/tmp/guiyi-v1.9.15-released-status
```

- [ ] **Step 2: 更新 STATUS**

只写已证明事实：正式 Release 为 `v1.9.15@$RELEASE_COMMIT_SHA`；Runtime 仍为旧版本；PF2611 plan/apply、Runtime promotion、自然 Event、provider acceptance、微信确认仍 pending；保留历史 `LIVE_DOMINANT_MISMATCH`。

- [ ] **Step 3: 验证并合入 develop**

```bash
openspec validate --specs --strict --no-interactive
python3 scripts/engineering/secret_scan.py --json
git diff --check
git add STATUS.md
git commit -m "docs(status): record v1.9.15 release"
git push -u origin docs/v1.9.15-released-status
STATUS_PR_URL="$(gh pr create \
  --base develop \
  --head docs/v1.9.15-released-status \
  --title 'docs(status): record v1.9.15 release' \
  --body 'Records the released v1.9.15 identity and leaves data/runtime/natural gates pending.')"
STATUS_PR="${STATUS_PR_URL##*/}"
gh pr merge "$STATUS_PR" --merge
```

---

### Task 8: Gate C — exact-tag 自动解析 PF2611 `through` 并生成 read-only plan

**Files:**
- Read only: exact tag checkout、production Catalog/Calendar/Session、Canonical metadata。

**Interfaces:**
- Produces: `$THROUGH_DATE`, `$PLAN_SHA256`, `/tmp/guiyi-pf2611-v1.9.15-plan.json`。

- [ ] **Step 1: 建立 exact-tag plan worktree**

```bash
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"
git fetch origin --tags
RELEASE_COMMIT_SHA="$(git rev-parse 'v1.9.15^{}')"
test ! -e /private/tmp/guiyi-v1.9.15-plan
git worktree add --detach /private/tmp/guiyi-v1.9.15-plan v1.9.15
cd /private/tmp/guiyi-v1.9.15-plan
test "$(git rev-parse HEAD)" = "$RELEASE_COMMIT_SHA"
git status --short
uv sync --project services/quant-api --locked
```

- [ ] **Step 2: 只读解析 latest complete day**

```bash
THROUGH_DATE="$({
PYTHONPATH=services/quant-api:packages/quant-core \
  uv run --project services/quant-api python - <<'PY'
from app.core.env import PROJECT_ROOT
from app.db.session import SessionLocal
from app.market_data.coverage_source import DatabaseCoverageSource

with SessionLocal() as session:
    source = DatabaseCoverageSource(
        session,
        PROJECT_ROOT / "data/universe/product_window_starts.csv",
        history_floor_path=PROJECT_ROOT / "data/universe/active_history_floor.txt",
    )
    print(source.latest_complete_day(("pf",)).isoformat())
PY
} | tail -1)"
printf 'THROUGH_DATE=%s\n' "$THROUGH_DATE"
python3 - "$THROUGH_DATE" <<'PY'
from datetime import date
import sys
value = date.fromisoformat(sys.argv[1])
assert value >= date(2026, 9, 3), value
print("THROUGH_DATE_POLICY_OK=1")
PY
```

`> 2026-09-03` 直接继续；`< 2026-09-03` 或解析失败立即停止。

- [ ] **Step 3: dry-run plan**

```bash
PYTHONPATH=services/quant-api:packages/quant-core \
  uv run --project services/quant-api guiyi data contract-warmup \
  --symbol pf \
  --contract PF2611 \
  --through "$THROUGH_DATE" \
  > /tmp/guiyi-pf2611-v1.9.15-plan.json
cat /tmp/guiyi-pf2611-v1.9.15-plan.json
```

- [ ] **Step 4: schema/identity/hash 验证**

```bash
PLAN_SHA256="$(python3 - "$THROUGH_DATE" <<'PY'
import json, sys
from pathlib import Path
through = sys.argv[1]
p = json.loads(Path('/tmp/guiyi-pf2611-v1.9.15-plan.json').read_text())
assert p['schema_version'] == 2
assert p['command'] == 'data.contract-warmup'
assert p['readonly'] is True
assert p['symbol'] == 'pf'
assert p['contract'] == 'PF2611'
assert p['requested_window']['through'] == through
assert p['applied'] == 0
assert p['blocked'] == 0
assert p['failed'] == 0
assert len(p['plan_sha256']) == 64
print(p['plan_sha256'])
PY
)"
printf 'PLAN_SHA256=%s\n' "$PLAN_SHA256"
```

- [ ] **Step 5: Gate C evidence**

记录 exact release、listed/effective window、`$THROUGH_DATE`、direct/derived counts、expected bars、provider request count、`$PLAN_SHA256`。完成状态：`WARMUP_PLAN_READY`。

---

### Task 9: Gate D — PF2611 真实 RQData / Canonical apply

**Files:**
- Production mutation: PF2611 physical Canonical partitions only。

**Interfaces:**
- Consumes: `v1.9.15@$RELEASE_COMMIT_SHA`, `$THROUGH_DATE`, `$PLAN_SHA256`。
- Produces: `PF2611_WARMUP_APPLIED_AND_VERIFIED`。

- [ ] **Step 1: 打印 exact apply identity 并停止等待 Owner 授权**

```bash
printf 'release=v1.9.15@%s\nsymbol=pf\ncontract=PF2611\nthrough=%s\nplan_sha256=%s\n' \
  "$RELEASE_COMMIT_SHA" "$THROUGH_DATE" "$PLAN_SHA256"
```

日期更晚不产生额外 Gate；但没有本轮针对该 exact plan 的真实 apply 授权，不执行 Step 3。

- [ ] **Step 2: mutation 前只读保存 Rule/Scope/Event baseline**

```bash
PYTHONPATH=services/quant-api:packages/quant-core \
  uv run --project services/quant-api python - <<'PY' \
  > /tmp/guiyi-subing-before-warmup.json
import hashlib, json
from sqlalchemy import func, select
from app.alerts.models import AlertEvent, AlertRule
from app.alerts.registry import SUBING_THS_ALERT_RULE_CODE
from app.db.session import SessionLocal

with SessionLocal() as session:
    rule = session.scalar(select(AlertRule).where(AlertRule.rule_code == SUBING_THS_ALERT_RULE_CODE))
    assert rule is not None
    scope = rule.scope_product_frequencies
    encoded = json.dumps(scope, ensure_ascii=False, sort_keys=True, separators=(",", ":")).encode()
    count = session.scalar(select(func.count()).select_from(AlertEvent).where(AlertEvent.rule_id == rule.id))
    print(json.dumps({
        'enabled': rule.enabled,
        'scope_sha256': hashlib.sha256(encoded).hexdigest(),
        'event_count': int(count or 0),
    }, sort_keys=True))
PY
cat /tmp/guiyi-subing-before-warmup.json
```

Expected: enabled=true；scope hash 与当前批准 authority 一致。

- [ ] **Step 3: 授权后执行一次 hash-locked apply**

```bash
PYTHONPATH=services/quant-api:packages/quant-core \
  uv run --project services/quant-api guiyi data contract-warmup \
  --symbol pf \
  --contract PF2611 \
  --through "$THROUGH_DATE" \
  --expected-plan-sha256 "$PLAN_SHA256" \
  --apply \
  > /tmp/guiyi-pf2611-v1.9.15-apply.json
cat /tmp/guiyi-pf2611-v1.9.15-apply.json
```

非零退出、`failed>0` 或 `blocked>0`：立即停止，不自动重试。

- [ ] **Step 4: production read-only audit**

```bash
PYTHONPATH=services/quant-api:packages/quant-core \
  uv run --project services/quant-api guiyi data audit \
  --symbol pf \
  --through "$THROUGH_DATE" \
  > /tmp/guiyi-pf2611-v1.9.15-audit.json
cat /tmp/guiyi-pf2611-v1.9.15-audit.json
```

Expected: 无阻断 finding。

- [ ] **Step 5: exact physical 15m readback**

```bash
PYTHONPATH=services/quant-api:packages/quant-core \
  uv run --project services/quant-api python - "$THROUGH_DATE" <<'PY'
from datetime import date, timedelta
import sys
from app.db.session import SessionLocal
from app.market_data.composition import build_market_data_service
from app.market_data.domain import BarFrequency, ContractTradingDayQuery

through = date.fromisoformat(sys.argv[1])
with SessionLocal() as session:
    service = build_market_data_service(session)
    fact = service.catalog.contract_fact('pf', 'PF2611')
    effective_through = min(through, fact.expired_date - timedelta(days=1))
    result = service.query_contract_trading_days(
        ContractTradingDayQuery(
            symbol='pf',
            contract='PF2611',
            frequency=BarFrequency.M15,
            since=fact.listed_date,
            through=effective_through,
        )
    )
    assert result.bars
    assert all(bar.contract == 'PF2611' for bar in result.bars)
    assert result.bars[0].trading_day >= fact.listed_date
    assert result.bars[-1].trading_day <= effective_through
    print('PF2611_15M_PHYSICAL_READ_OK', len(result.bars))
PY
```

- [ ] **Step 6: Rule/Scope/Event post-readback 必须与 baseline 完全一致**

使用 Step 2 同一脚本写 `/tmp/guiyi-subing-after-warmup.json`，然后：

```bash
cmp /tmp/guiyi-subing-before-warmup.json /tmp/guiyi-subing-after-warmup.json
```

- [ ] **Step 7: partial/failed path**

如 apply partial/failed：保留已原子成功合法分区，回 Task 8 生成新 exact plan/hash；真实重试需新的 Owner 执行意图。

- [ ] **Step 8: STATUS 记录**

通过普通 develop docs PR 只记录 exact release、PF2611、through、warm-up apply/readback 已完成；Runtime promotion/natural Event 仍 pending。

---

### Task 10: Gate E — exact-tag 五项 Runtime promotion

**Files:**
- External Runtime: API / Web / Live / after-market / Alert launchd。

**Interfaces:**
- Consumes: `v1.9.15@$RELEASE_COMMIT_SHA` + verified PF2611 warm-up。
- Produces: `$RUNTIME_ROOT`, `$ROLLBACK_ROOT`, `$PROMOTION_TIME`, `RUNTIME_PROMOTED_V1_9_15`。

- [ ] **Step 1: fresh 读取现役 Runtime/rollback identity**

```bash
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"
./scripts/ops/macos/local-services-status.sh
ROLLBACK_ROOT="$(plutil -extract EnvironmentVariables.GUIYI_PROJECT_ROOT raw -o - \
  "$HOME/Library/LaunchAgents/com.guiyi.quant-api.plist")"
printf 'ROLLBACK_ROOT=%s\n' "$ROLLBACK_ROOT"
```

继续对 API/Web/Live/after-market/Alert 五个 installed plist 读取 `GUIYI_PROJECT_ROOT` 与 `GUIYI_RUNTIME_COMMIT`，要求同一现役 root/commit；不一致则停止。

- [ ] **Step 2: exact new Runtime root**

```bash
RUNTIME_ROOT="/Volumes/扩展盘/guiyi-quant-runtime-v1.9.15-r1"
cd "$REPO_ROOT"
git fetch origin --tags
if [ -e "$RUNTIME_ROOT" ]; then
  test -d "$RUNTIME_ROOT/.git" || test -f "$RUNTIME_ROOT/.git"
  test "$(git -C "$RUNTIME_ROOT" rev-parse HEAD)" = "$RELEASE_COMMIT_SHA"
  test -z "$(git -C "$RUNTIME_ROOT" status --short)"
else
  git worktree add --detach "$RUNTIME_ROOT" v1.9.15
fi
cd "$RUNTIME_ROOT"
test "$(git rev-parse HEAD)" = "$RELEASE_COMMIT_SHA"
```

- [ ] **Step 3: render-only**

```bash
./scripts/ops/macos/install-local-services.sh --render-only
```

- [ ] **Step 4: 打印 Runtime Gate identity 并停止等待 Owner 授权**

```bash
printf 'release=v1.9.15@%s\nruntime_root=%s\nrollback_root=%s\nservices=API,Web,Live,after-market,Alert\n' \
  "$RELEASE_COMMIT_SHA" "$RUNTIME_ROOT" "$ROLLBACK_ROOT"
```

没有本轮明确 Runtime promotion 授权，不执行 Step 5。一次 rollback 只有 Owner 在该授权中明确允许时才可执行。

- [ ] **Step 5: Market Runtime preflight**

```bash
./scripts/ops/macos/run-local-service.sh market-runtime-preflight
```

Pass reason 只能为 `snapshot_ready | before_first_session | after_market_complete | non_trading_interval`；任何 block reason 停止，零 promotion mutation。

- [ ] **Step 6: 切换 API/Web**

```bash
./scripts/ops/macos/install-local-services.sh --confirm-load
```

- [ ] **Step 7: 切换 Live + after-market**

```bash
./scripts/ops/macos/install-local-services.sh --confirm-market-runtime
```

- [ ] **Step 8: 切换 Alert**

保持现有 Git 外通知配置环境，不读取/打印其内容：

```bash
./scripts/ops/macos/install-local-services.sh --confirm-alert-runtime
```

- [ ] **Step 9: promotion 时间与 readback**

```bash
PROMOTION_TIME="$(date -u '+%Y-%m-%dT%H:%M:%SZ')"
./scripts/ops/macos/local-services-status.sh
./scripts/ops/network/local-tunnel-healthcheck.sh
printf 'PROMOTION_TIME=%s\n' "$PROMOTION_TIME"
```

Expected: 五项 loaded root/commit 均为 `$RUNTIME_ROOT` / `$RELEASE_COMMIT_SHA`；API/Web/Live/Alert running，after-market 按调度状态。

- [ ] **Step 10: failure/rollback path**

不自动 retry、不改 Scope、不补发通知。只有 Step 4 授权明确包含一次 rollback 时，才从 `$ROLLBACK_ROOT` 执行一次对应 installer rollback；rollback 后停止。

- [ ] **Step 11: STATUS 记录**

普通 develop docs PR 只记录 `RUNTIME_PROMOTED_V1_9_15 / NATURAL_EVIDENCE_PENDING`；保留 2026-09-03 历史失败。

---

### Task 11: Gate F — 自然 completed SuBing 15m evaluation

**Files:**
- No code/data mutation。
- Read: Runtime health、Live snapshot、Alert rule status、Market identity。

**Interfaces:**
- Produces: `RUNTIME_NATURAL_EVALUATION_HEALTHY` 或 blocker。

- [ ] **Step 1: 等待下一条自然 operational completed 15m**

不得手工 after-market、manual alert trigger、synthetic、replay、backfill。

- [ ] **Step 2: service identity readback**

```bash
cd "$RUNTIME_ROOT"
./scripts/ops/macos/local-services-status.sh
```

五项必须仍指向 `$RELEASE_COMMIT_SHA`。

- [ ] **Step 3: 读取本地 `/api/runtime/health` / Alert status**

确认 Alert heartbeat 前进、SuBing `last_evaluated_bar_at` 前进、per-rule processing error 为空、Live subscription/phase 与 operational universe 一致。

- [ ] **Step 4: current physical contract identity**

当前 rank1 contract 必须与 evaluator replay contract 一致；如果是 PF2611，必须从 PF2611 自身 lifecycle prefix 重建，不能继承旧合约递归状态。

- [ ] **Step 5: no Candidate 是合法中间态**

无 Candidate 时记录：

```text
RUNTIME_NATURAL_EVALUATION_HEALTHY
SUBING_NATURAL_EVENT_PENDING
```

继续自然等待，不人为制造信号。

- [ ] **Step 6: STATUS 记录**

只写实际自然 evaluation 事实，Event/PushPlus/微信仍 pending。

---

### Task 12: Gate G11 — 自然 SuBing Event + one-shot PushPlus provider acceptance

**Files:**
- Production facts: existing `alert_events` + Runtime status only。

**Interfaces:**
- Produces: `$EVENT_SYMBOL`, `$EVENT_CONTRACT`, `$EVENT_DIRECTION`, `$EVENT_BAR_END`, `$EVENT_DETECTED_AT`，以及 `PUSHPLUS_PROVIDER_ACCEPTED` 或 pending。

- [ ] **Step 1: 继续等待自然 Candidate**

只接受 exact promoted Runtime 自然产生、`detected_at > $PROMOTION_TIME` 的 completed 15m Candidate。

- [ ] **Step 2: 通过只读 Alert API 读取 Event**

从 `/api/alerts/current-events` 或 `/api/alerts/events` 提取并冻结：

```text
rule_code=subing_ths_alert_15m_v1
frequency=15m
EVENT_SYMBOL
EVENT_CONTRACT
EVENT_DIRECTION=buy|sell
EVENT_BAR_END
EVENT_DETECTED_AT
```

并验证 `$EVENT_DETECTED_AT > $PROMOTION_TIME`、contract 与当时 rank1 identity 一致。

- [ ] **Step 3: 证明 Event-first**

读取 Event `notification_attempted_at` 与 Runtime transport evidence，确认 Event 已持久化后才发生唯一一次 transport attempt；不得手工更新 Event。

- [ ] **Step 4: provider failure path**

formatter/PushPlus/provider 失败：Event 保留，不 retry、不 manual send、不 replay；等待**未来新的自然 Event**。

- [ ] **Step 5: provider accepted path**

若同一 Event provider accepted，记录上述 Event identity 与 acceptance 时间；不得记录 secret，也不得写成微信送达。

- [ ] **Step 6: STATUS 记录**

只推进到：

```text
SUBING_NATURAL_EVENT_PERSISTED
PUSHPLUS_PROVIDER_ACCEPTED
WECHAT_DELIVERY_CONFIRMATION_PENDING
```

---

### Task 13: Gate G12 — Owner 微信确认并关闭 Issue #307

**Files:**
- Modify: `STATUS.md`
- GitHub: Issue `#307`

**Interfaces:**
- Consumes: Task 12 同一自然 Event identity。
- Produces: `SUBING_NATURAL_CLOSURE_COMPLETE`。

- [ ] **Step 1: 向 Owner 展示可核对非敏感字段**

展示 `$EVENT_SYMBOL`、`$EVENT_CONTRACT`、`$EVENT_DIRECTION`、`$EVENT_BAR_END` 和通知业务摘要；不得展示 token/Topic secret。

- [ ] **Step 2: 等待 Owner 明确确认微信实际收到**

provider accepted 不能替代。未确认就停止。

- [ ] **Step 3: Owner 确认后更新 STATUS**

在 fresh develop docs branch 记录：

```text
v1.9.15 RELEASED
v1.9.15 RUNTIME_READY
SUBING_WECHAT_DELIVERY_CONFIRMED
SUBING_NATURAL_CLOSURE_COMPLETE
```

同时保留历史 `LIVE_DOMINANT_MISMATCH` 和同一自然 Event identity。

- [ ] **Step 4: 验证/提交/合入 develop**

```bash
openspec validate --specs --strict --no-interactive
python3 scripts/engineering/secret_scan.py --json
git diff --check
git add STATUS.md
git commit -m "docs(status): close subing natural notification gate"
git push -u origin docs/subing-natural-closure-complete
FINAL_STATUS_PR_URL="$(gh pr create \
  --base develop \
  --head docs/subing-natural-closure-complete \
  --title 'docs(status): close SuBing natural notification gate' \
  --body 'Records the verified v1.9.15 Runtime/natural Event/provider acceptance/Owner WeChat delivery closure.')"
FINAL_STATUS_PR="${FINAL_STATUS_PR_URL##*/}"
gh pr merge "$FINAL_STATUS_PR" --merge
```

- [ ] **Step 5: 关闭 Issue #307**

Issue comment 记录：`v1.9.15@$RELEASE_COMMIT_SHA`、同一自然 Event 的非敏感 identity、provider acceptance、Owner delivery confirmation，并明确 synthetic/replay/manual send 未用于验收；然后关闭 Issue #307 为 completed。

---

## Final Verification Checklist

- [ ] `v1.9.15` release tree == reviewed full-develop RC tree。
- [ ] full backend/Newow/engineering/Web/E2E/OpenSpec/security 均来自同一 exact RC fresh run。
- [ ] Standards Review 与 Spec Review 均针对同一 exact head，P1/P2=0。
- [ ] GitHub checks 若不存在，明确记录 `NO_CHECKS_REPORTED`。
- [ ] PF2611 `$THROUGH_DATE` 来自 `latest_complete_day(("pf",))`，且 `>= 2026-09-03`；更晚日期没有触发额外日期 Gate。
- [ ] warm-up apply 使用 exact `$PLAN_SHA256`，且只有一次已授权 attempt。
- [ ] warm-up 前后 SuBing Rule/Scope/Event baseline 不变。
- [ ] PF2611 exact physical 15m lifecycle readback 成功。
- [ ] 五项 Runtime 指向同一 `v1.9.15` exact commit/root。
- [ ] promotion 后自然 completed 15m evaluation 健康。
- [ ] 自然 Candidate 先形成 immutable Event，再最多一次 PushPlus attempt。
- [ ] provider accepted 与 Owner 微信确认分开记录。
- [ ] 无 synthetic/replay/backfill/manual send 代替自然闭环。
- [ ] `auto_order=false`；无订单、账户、模拟交易、Newow Runtime 扩权。
- [ ] `STATUS.md` 只记录已发生事实，Issue #307 只在 G12 后关闭。

## Completion State

只有 Final Verification Checklist 全部满足，才允许最终结论：

```text
v1.9.15 RELEASED
v1.9.15 RUNTIME_READY
SUBING_NATURAL_CLOSURE_COMPLETE
```

任一 Gate 未完成时使用最窄中间状态，不越级声明完成。