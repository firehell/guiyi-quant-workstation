# Market Web 发布前十一项验收报告

日期：2026-09-15（Asia/Shanghai）

候选：`develop@0963cef3427d195fc4ec34ec818bc97dd570f23d`

范围：只读代码、测试、浏览器与现役状态复核。未执行 RQData 下载、Canonical/数据库/Redis 写入、
Scope/Rule/通知变更、main/tag/Release 或 Runtime switch。

## 结论

状态为 `CODE_COMPLETE / TEST_COMPLETE / REVIEW_COMPLETE / RELEASE_CANDIDATE / EXTERNAL_GATE_PENDING`。代码与展示层验收通过；
JM 的真实物理合约历史缺口没有被页面降级、fixture 或合约替换掩盖，仍须独立数据修复 Gate。候选不属于
当前 `v1.10.10` Release/Runtime。

## 十一项验收矩阵

| # | 验收项 | 代码/fixture 证据 | 真实只读 API/Chrome 证据 | 结论 |
|---:|---|---|---|---|
| 1 | JM SuBing 不再只显示不可判定 | 后端返回 allowlist 的 stage/reason/context；页面显示公开诊断 | JM 返回 409：`physical_contract_replay / DATASET_OR_PARTITION_MISSING`，合约 JM2601，expected_count=4882 | 诊断闭环通过；数据 Gate pending |
| 2 | AU/AG/JM 大小写一致 | API 在 cache/service 前统一归一为大写；六组参数测试 | 最终候选对 AU/au、AG/ag、JM/jm 做成对 HTTP 读回 | 通过 |
| 3 | 首页低于 10 秒 | 60 行、显式重载、双并发与错误恢复回归 | 60/60；冷请求 8885ms，连续读取 8731/9409/9488ms，双并发 8916ms，一次失败后恢复用例 9.3s | 通过 |
| 4 | 品种选择器搜索/键盘/焦点稳定 | unit + fixture E2E 覆盖程序化 focus 一次抑制、键盘选择和不重开 | 最终候选 Chrome 搜索 JM 并进入详情，新页面下拉未重开 | 通过 |
| 5 | SuBing 四类事实不混淆 | Scope、Rule/Runtime、全局评价、当前品种 stored Event 分开；无 Event 不解释为中性 | 候选仅开放历史 reference；正式 health/Event 另行只读读回，未伪装成候选快照 | 通过 |
| 6 | Decimal 显示无 Number 精度损失 | 字符串算法覆盖指数、超大值、极小值、半入舍入和负零 | AU/JM 页面数值渲染与 raw 属性保留 | 通过 |
| 7 | 时间与术语统一 | 可见 Event/history/explanation 时间均转北京时间；reason/动作中文化 | AU/JM 候选页面未暴露内部 reason code，时间按北京时间展示 | 通过 |
| 8 | 移除无依据 5 日涨跌 | 详情页不再渲染静态 `—` 指标；单测锁定 | 真实详情页无该伪指标 | 通过 |
| 9 | W1 未完成区间可显式切换 | 周一、节假日、跨年测试；只使用服务端边界 | AU 点击“使用最近完整统计区间”后切至 2026-09-11，并重新请求 | 通过 |
| 10 | STATUS/Canonical/Testing 同步 | 本报告、STATUS、两份 OpenSpec、ARCHITECTURE、TESTING 同步 | v1.10.10 tag/main/Runtime identity 与 22:15 服务状态分别读回 | 通过 |
| 11 | 密集信号标记避让 | 纯布局算法覆盖 selected priority、稳定性、碰撞、边界、缩放与 raw id | AU trend/oscillation/main-rise 分别加载 32/5/1 个真实 callout；1920、1440、390 与全屏边界均通过 | 通过 |

## 真实数据边界

固定 `as_of=2026-09-14T11:00:00Z` 的只读复核中：

- AU 三策略 W1 为 ready，七周期 Free 图表可读；本轮不重复声明未在当前 exact-commit 输出中回读的端点统计。
- JM 三策略 W1 均保持 unavailable；SuBing 长窗口在 `JM2601` 的 15m 物理重放阶段缺少数据集或分区，
  expected_count=4882。本轮候选验收不把该有界诊断扩写成未经重新盘点的缺失日期或端点数。
- 另一物理合约 `JM2609 / 15m / 2025-09-15..2026-08-18` 曾按精确授权修复；其历史 apply 与当时默认页面读回见
  [JM2609 回执](JM2609-15m-apply-receipt.md)。这不关闭上述 `JM2601` 长窗口缺口，也不替代本候选的只读复核。
- 这与既有 Newow JM W1 历史缺口是两条独立证据，不得互相替代。

## 验证摘要

- 后端相关变更在提交前定向验证通过；本次 exact-commit 真实候选 4/4 直接覆盖候选 API 路径。
- Web unit：615 passed、1 个既有可选 golden skip；typecheck、production build 通过。
- 完整 Web E2E：155 passed、7 个候选专用用例按默认环境跳过、0 failed。
- exact-commit 真实候选浏览器：4/4 passed，覆盖 60 品种首页、AU/JM Newow/SuBing、最近完整周、
  callout 桌面/移动/全屏与失败恢复。
- 独立 Review 固定检查 `01a68f175..0963cef34`，0 findings；复核单测 20/20 与 `git diff --check` 通过。

## Release/Runtime 分界

当前正式身份为 `v1.10.10@b49e2499de654092b48e60e181102c02e16ce89f`。API/Web、Live/Alert 与当日
自然盘后已只读通过；provider accepted 不证明用户实际收件，weekly audit 尚未自然运行，因此不声明
`RUNTIME_READY`。本候选未进入 main/tag/Release/Runtime。

## 唯一最小下一步

由 owner 单独授权当前候选的 main/tag/GitHub Release；JM 数据修复仍走独立数据 Gate。
