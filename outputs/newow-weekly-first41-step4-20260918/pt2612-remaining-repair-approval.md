# PT2612 剩余 W1 前缀修复授权包

状态：**只读计划完成，尚未授权或执行本批次写入**。先前获批的
`2025-12-15～19` 批次已一次执行成功（`applied=2`、provider 请求 2 次）；
写入后 D1 的 13 根和 SHA-256 完全不变，W1 新增 2025-12-19 一根，
MDS 周质量校验通过。三策略真实默认首载随后均转为在**下一周
2025-12-26**报 `REPLAY_ENDPOINTS_MISSING`，因此先前的精确授权已消费，
不得用于继续补数。详见本目录的 `pt2612-apply-result.json`、
`pt2612-postapply-readback.json`、`pt2612-postapply-snapshots.json` 和
`pt-postapply-browser.txt`。

## 完整依赖边界

只读 [owner 依赖检查](pt-weekly-owner-dependencies.json)显示，PT 的四段
rank1 owner 中 `PT2606`、`PT2608`、`PT2610` 均为 `DATA_READY`；
`PT2612` 的 rank1 区段为 2026-09-17～18，但其 W1 策略预热要求从
合约上市起点 `2025-12-15` 读取完整物理合约前缀。现役 D1 的 10 个月分区
都有数据；现役 W1 仅有 2025-12-19（刚修复）和 2026-09-18 两根，
内部缺 37 根。精确[剩余计划](pt2612-remaining-readonly-plan.json)和
[当前活动分区清单及 SHA](pt2612-remaining-before-readback.json)已保存。
通用产品 audit 在 rank1 分区范围报告 `passed`，不覆盖此处完整合约预热前缀，
不能代替该依赖检查。

## 请求的第二个独立批次

| 维度 | 精确范围 |
| --- | --- |
| 环境 | 本机生产 Market Catalog、既有 Canonical 根；仅使用现有 `contract_warmup` 维护实现 |
| 身份 | `symbol=pt`、`contract=PT2612`、`frequency=1w`、`through=2026-09-18` |
| 完整计划 SHA-256 | `843bb24f1979e3e399eafc6c71c3136260440a3fd4c8b3a832f4b4c98f6883fe` |
| 目标分区 | 仅该合约 `1d` 和 `1w`，2025-12 及 2026-01～09 的各 10 个活动月分区，共 20 个直接目标、0 个派生目标 |
| 来源及预算 | RQData 最多 20 次请求：重取 177 根既有 D1 作周线一致性校验，获取 37 根缺失 W1；无自动重试 |
| 写入方式 | 新不可变 Canonical candidate；完成所有 D1/W1 一致性和质量校验后，在一次 Catalog 事务中激活 20 目标 |

所有现役 D1 已有对应日线事实；计划的 D1 `missing_bar_count` 是重新获取并
核验范围，**不表示原 D1 缺失**。计划仅包含 `pt/PT2612`，不扩展至其他
合约、品种、周期、月份或当前周之后。执行调用省略可选的首页投影失效钩子，
避免删除授权范围外的生产投影文件；维护锁、哈希复核和原子提交保持启用。

执行前必须重读这 20 个活动分区与校验和、维护锁和计划哈希。任何计划变化、
来源冲突、预算超出或维护锁占用均停止，不拆分或盲目重试。若来源或校验阶段
失败，先只读确认活动 Catalog 未变化；孤立不可变 candidate 不自行删除。
若提交结果不明，停止并只读对账，不重放本批次。成功后逐分区检查现有 D1
内容未变、37 根 W1 缺口补齐，并复验 PT 三策略的无固定 `as_of` 默认页面、
参考交易/收益、比较器及 PT D1 回归；合法预热仍不得记策略 READY。

此批次不含 main/tag/release、Runtime promotion、正式消费者切换、真实通知
或生产数据删除。若提交后验收失败，保留上述写入前分区文件和 SHA 作为恢复
锚点，另拟精确恢复计划并取得授权，不自动回滚。
