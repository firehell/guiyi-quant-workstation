---
description: 项目全局规范 — 所有文件均适用
globs: ["**/*"]
alwaysApply: true
---

# 归一量化工作站 — 项目全局规范

---
description: 归一量化项目全局规则
globs:
  - "**/*"
alwaysApply: true
---

这是本地国内期货量化研究与回测工作台。

开发时遵守：
- 不做第一版全自动实盘。
- 先做数据、K线、策略、回测、报告、复盘、信号闭环。
- 所有交易逻辑必须可解释、可回测、可复盘。
- 回测必须避免未来函数和数据泄露。
- 前端使用 Vue 3 + Vite + TypeScript + Naive UI。
- 后端使用 FastAPI + PostgreSQL + Parquet + DuckDB。
- 大改前先 Git checkpoint。
- 不允许提交 API Key、账户密码、交易密码。
