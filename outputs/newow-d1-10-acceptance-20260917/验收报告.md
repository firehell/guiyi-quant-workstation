# 牛哇 D1 十品种候选验收（只读）

- 代码身份：`develop@0afdf1144010c135fbe8ee7a0f3dbb7d06f5238f`。候选 API identity 已读回该 SHA；Web 与 API 分别仅绑定本机 `127.0.0.1:5174/8010`，验收后已关闭。
- 冻结截止：`2026-09-15T07:00:00.000001+00:00`；品种：`RS SM SN SR SS TA UR V Y ZN`；组合：各品种趋势、震荡、主升浪 × `1d`，共 30 页。
- 数据与计算：对十品种分别运行 `guiyi data newow-readiness --symbol <symbol> --frequency 1d --matrix --compact --as-of 2026-09-15T07:00:00.000001+00:00 --max-work 10000 --timeout-seconds 300`。10/10 `audited`、`complete=true`、`budget_exhausted=false`；30/30 D1 `main/chart/reference=READY`；补数目标 0，逐项 `provider_requests=0`、`writes=0`。
- 自然页面：通过隔离只读候选预览和真实浏览器，按上述顺序逐页首次导航并等待 `networkidle`。30/30 页 `data-chart-state=ready`，参考收益摘要含“收益合计”，逐页 console error 为 0。旧提交 `c4c61366` 上 RS 三页曾因 `NEWOW_SNAPSHOT_GENERATION_CONFLICT` 红灯，不计入通过；修复提交后 30 页全部重新验收。
- RS 根因与修复：主图和完整参考历史的不同读取窗口，使未来区段的同一物理预热 Bar 错借较早 owner 起点进入快照证明，产生伪冲突。`0afdf114` 从 Bar proof 值移除该冗余字段并升级内部 proof 身份至 v5；Bar 内容、segment ID、eligible 和共享换月边界继续比较。新增回归先失败后通过，真实 RS 三策略 chart-token→reference 均 READY 且 token 相同。独立 Review 无 Confirmed Issue。
- 验证：`test_product_service.py`、`test_product_snapshot_cache.py`、`test_older_chart_windows.py`、`test_market_newow_product_api.py` 合计 116 passed；定向 Ruff 通过；`git diff --check` 通过；secret scan 0 findings。更大 Newow 测试组未取得完整终态，不声明全套通过。

## 边界与未完成 Gate

本报告只验收当前已集成的 `newow_futures_effective_observation_v1` / `newow_futures_segment_interrupt_no_trade_v2` 只读候选，不是账户收益、因果收益、Release 或 Runtime 验收。来源价格不可用后的质量分段、重新预热和 `DATA_INTERRUPTED` 是独立工作树中的待集成规则；本报告不证明该新规则已在十品种通过。`origin/develop` 推送被宿主安全审查因远端归属未获本轮明确确认而拒绝，本地提交尚未同步；未操作 main/tag、正式服务、真实 RQData、Canonical 或 Catalog。
