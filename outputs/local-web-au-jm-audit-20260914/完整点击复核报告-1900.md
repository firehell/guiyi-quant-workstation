# 黄金 / 焦煤本地 Web 完整点击复核

2026-09-14 19:00–19:19（北京时间）。状态：COMPLETED（本轮巡检和问题整理）；产品页面验收结论：**阻塞**。

本轮通过计算机接口实际操作 Chrome 的 `http://127.0.0.1:5173`，以黄金 AU/AU2610、焦煤 JM/JM2701 检查主要入口。Chrome 专用连接失败后，计算机接口可以正常读取和点击；该工具连接故障不计入产品问题。保留此前 17:39 和 18:55 的报告，不将旧截图或接口状态冒充本次结果。当前问题以本报告为准。

共跟踪 19 项：**18 项当前问题或体验缺口（6 项 P1、9 项 P2、3 项 P3），1 项旧问题本次未复现**。这是现象清单，不是 18 个独立根因；01/02/03 关联首页链路，05/06 关联焦煤预热输入。

## 当前事实和范围

- 开发目录为 develop@7982c8c921245853d52c0740e20278ec144e7464。已有其他数据恢复输出及 worktree 未修改。
- 实际 API `/health` 返回 version=1.10.10、readonly=true、status=ok；v1.10.10 Runtime 目录 HEAD exact tag 为 v1.10.10。本轮未审计完整安装 receipt 和六服务身份，不由此声明 Runtime Ready。
- 黄金、焦煤自由看盘的七个周期均实际切换，数据状态正常；分钟/日线截至 9/14，完整周线截至 9/11。
- 黄金牛哇三策略当前主图可以显示；参考区可展示历史记录，但默认统计窗口处于 weekly partial/warming。黄金苏冰历史参考已实际显示表格。旧报告“两品种均不可用”的结论不再适用。
- 焦煤牛哇三策略当前主图、参考交易及最近历史恢复均实际失败；苏冰历史参考仍失败。

## 点击覆盖

| 区域 | 本轮实际操作 | 结果 / 限制 |
|---|---|---|
| 首页 | 回到市场、刷新、状态详情、首页搜索、重新加载地址、最后返回 | 驻留页为 0/60 stale 缓存及刷新失败；重新加载后无成功快照、骨架及不可用提示。无法完成表格排序或行内详情验收 |
| 消息 | 苏冰、火天大有、品种下拉、加载更多、日期起点改到 9/14 并恢复 9/8、点击 JM 5m 事件、回到最新、返回市场 | 类型筛选/分页有结果；正常日期筛选生效；消息跳到 JM 5m；返回恢复消息页。品种下拉只有“全部品种” |
| 品种选择 | 黄金详情打开选择器、搜索焦煤、点击选项、Esc 关闭 | 能切 JM；选择后下拉重新打开，覆盖页面 |
| 自由看盘 | AU/JM 各 1m/5m/15m/30m/60m/日K/周K，共 14 个周期入口 | 全部报价可用、数据正常；不等于所有历史合约完整 |
| 序列 | AU/JM 真实主力、主连、指定 AU2610/JM2701，再恢复真实主力 | 路由与合约切换成功 |
| 通用指标/数据 | 黄金指标设置、EMA10/21/60、Range、数据详情 | 开关生效；Range 周线预载不足有明确提示。前次遗留四个勾选本轮已关闭 |
| 黄金牛哇 | 震荡、趋势、主升浪周线；震荡历史快照；参考记录展开、统计来源；趋势过程提示、全屏与退出；四种辅助图层；查看依据、指标解读、杯柄说明；主升浪比较说明 | 图可显示，参考区有数据但默认 partial；标注重叠、精度、文案问题见清单。主升浪比较器明确 not applicable，不算加载故障 |
| 焦煤牛哇 | 三策略逐个进入；分别读取参考交易、点击最近可用历史快照；趋势比较说明 | 三策略当前/参考/恢复全部失败；图内进一步操作被数据缺口阻塞 |
| 火天大有 | 黄金 15m/60m；焦煤 15m/5m；信号、运行、数据、历史页签；消息定位与回到最新 | 黄金 60m、焦煤 15m Scope 已启用；焦煤 5m 历史事件可读；黄金 60m 窗口暂无事件不算故障 |
| 苏冰 | 两品种固定 15m；历史参考、运行详情、历史记录；黄金事件详情 | 黄金参考表可读、Event 可读；焦煤参考不可用；状态与详情文案问题见清单 |
| 路由 | 本轮直接访问 /market/chart、消息跳转、返回 | 无参数详情本轮自动恢复黄金自由看盘。旧记录的非法身份恢复属于此前时段，本轮不重复计通过 |

未穷举所有日期、历史分页、每根 K 线、键盘组合和屏幕尺寸。消息“开始日期晚于结束日期”的校验未取得可靠结果：日期弹窗的原生点击出现非预期跳转，无法排除工具定位问题，不据此认定产品缺陷。主升浪统计终点曾尝试改为 9/11 并应用，但切页前未读回最终结果，不声称该恢复方式已通过。没有用 fixture 或 mock 补足上述验收。

## 问题清单

### 01 · P1 · 首页失败或过期时完全失去品种行和入口

复现：首页 → 市场。开始时页面显示“可用 0/60、过期 60、缺失 0”，黑色系空表，另提示 cached stale/上一份成功快照、刷新失败。重新加载地址后没有上一份成功快照，仍无可用表格。

影响：无法从首页查看黄金/焦煤或通过表格进入详情；详情本身却能正常读取行情。该问题应保留上次可解释的行情和品种入口，不能通过伪造 freshness 解决。当前后端已经 ready，因此本次不能再解释成“后端当前 60 个品种全部过期”。

### 02 · P1 · 首页快照耗时超过前端 30 秒超时

本轮只读 GET home-overview **49.857 秒**才返回 HTTP 200、ready、60/60、stale=0、missing=0。现役 v1.10.10 的 request.ts 默认 timeout=30000。较早 18:55 的同接口记录为 37.189 秒。

影响：数据已经齐备，页面仍无法稳定拿到结果；驻留页面显示旧缓存，重新加载页面显示无快照。49.857 秒是本次真实服务耗时，不是浏览器 Network 面板测量；服务中更细的耗时来源尚未定位。

### 03 · P1 · 首页搜索和消息品种筛选一起不可用

首页搜索截图显示“目录加载失败，无法安全切换品种”。消息品种下拉本次实际展开只有“全部品种”，不能筛选 AU/JM。详情里的品种选择器却能列出 60 品种并切换。

补充接口 `/market/dominants` 200，仅 0.488 秒。不能归因为服务器没有黄金/焦煤元数据；需检查首页目录与快照的依赖、失败缓存和恢复。

### 04 · 本次未复现 · 自由看盘首次切牛哇进入未开放 D1

旧报告曾记录此问题。本轮黄金自由看盘 15m → 震荡策略，正确进入 frequency=1w；焦煤从 HTDY 切震荡也进入 1w。**不计入当前 18 项问题**，保留为加载时序场景的待复现记录，不能标为已修复或必现。

### 05 · P1 · 焦煤牛哇三个当前周线主图和参考交易全部阻塞

复现：焦煤 → 震荡/趋势/主升浪 → 1w；分别点击读取参考交易。

三者均显示“主图事实不可用”，原因统一为 `JM2305` 同合约预热历史缺失，首个缺失周线 `2022-05-20T07:00:00+00:00`，缺 **32 根**。参考区也报加载失败、没有可显示的已验证数值；趋势比较器被相同输入缺口阻塞。

较早接口诊断含 expected=45、actual=13；本轮浏览器重新确认的是缺 32 根。未逐分区审计 Canonical，不能将错误上下文直接等同于已定位物理文件根因。黄金三策略已经恢复，不再包含在本项阻塞范围中。

### 06 · P1 · 焦煤最近历史快照恢复三策略均失败

复现：上述每个策略分别点击“查看最近可用历史快照”，等待最终响应。

震荡、趋势、主升浪均显示 `NEWOW_HISTORICAL_SNAPSHOT_UNAVAILABLE`，限定范围内没有找到可用快照。当前路径与恢复路径都无法使用。只证明既有搜索范围内恢复失败，不代表所有历史日期都没有数据。本项与 05 关联，应合并定位输入问题。

### 07 · P1 · 焦煤苏冰历史参考交易不可用

复现：焦煤 → 苏冰预警 → 乐观参考交易。显示“历史参考不可用，请核查数据覆盖或重新读取。实际预警记录独立展示”。本轮正式小写 API 请求返回 409、`SUBING_REFERENCE_DATA_UNAVAILABLE`，0.397 秒。

历史参考重算无法验收；当前窗口暂无已保存 JM 苏冰 Event 是另一个独立事实，不能据此认定 Event 丢失。黄金本轮已显示参考交易表，不再列入本项。

### 08 · P2 · 选择品种后下拉重新展开

复现：黄金详情 → 切换品种 → 搜索焦煤 → 点击焦煤。路由已变 JM，但下拉重新列出全品种目录，必须 Esc 关闭后继续。

源码证据：ProductSelector.vue 的 choose → close → nextTick focus 输入框；输入框 @focus=show，构成重新展开路径。覆盖层可能接收原本要点页面的下一次点击；本轮确认“重开”，旧报告误选铝的后果没有再次制造。

### 09 · P2 · 苏冰预警状态固定写“状态不可判定”

两品种都显示“状态不可判定 · 当前 Scope 已启用 · 最近已评估”，展开运行信息又显示全局 ok、Rule 已评估到 9/14 15:00、错误类型 —。

useSubingAlertFacts.ts:56 在有效 Rule 分支硬编码“状态不可判定”。无法清楚区分策略状态、Rule 运行和事件事实，容易误读为当前运行故障。不能简单改成账户持仓状态，也不能以 Rule 最近 Event 时间冒充当前品种最新 Event。

### 10 · P2 · 价格和百分比精度直接透出原始长小数

黄金震荡标注有 `449.320000000000000000`、`988.880000000000000000`；趋势建仓价 `911.3633333333333`，胜率 `66.66666666666666666666666667%`，平均单笔/合计/浮动也有长尾。

黄金苏冰截图的白底参考动作同样有大量价格尾零，因此影响不限于牛哇。参考表格部分字段已有格式化，但图上和统计卡未统一。应只改展示，不截断底层 Decimal、改变公式或改收益口径。

### 11 · P2 · 图上参考动作标注重叠并裁切

黄金趋势默认长历史窗口，建仓/清仓卡片密集叠放；本次全屏截图仍有重叠，2023 年一处建仓卡片贴到主图下沿并截断。黄金震荡低位卡片也贴近主图下边界，苏冰密集白底标签有相同可读性压力。

影响：无法完整对照动作与 K 线。长数字进一步增加宽度。未发现证据足以认定 Marker 公式或配对结果错误，本项是布局验收问题。

### 12 · P2 · 时区、中文名称和内部标识混用

报价区用北京时间；苏冰事件/运行详情直接显示 ISO UTC；趋势过程提示直接展示 `NEWOW_ESCAPE_D2`；市场详情板块展示 `precious`/`black`；参考区有 Performance window、reference cutoff、warming、OWNER_BOUNDARY 等未解释值。

同一事件不能直接与北京时间图表对照，技术代码占据日常阅读区域。UTC 和稳定 ID 作为底层事实合理，应在用户展示时转换或提供明确的中文解释。

### 13 · P2 · “5日涨跌”是固定占位，无法区分未实现和缺数

黄金数据详情/HTDY 行情扩展实际显示 —。源码 marketDetailViewModel.ts:185 将此字段固定为 —，没有绑定数据，而 home-overview 已有 price_change_5d。

这不是全部行情数据缺失。需明确字段来源或移除未实现占位；本轮没有修改计算或新增取数链。

### 14 · P3 · 苏冰预警详情没有增量核对信息

黄金 → 苏冰 → 历史记录 → 点击 9/9 的 S↑。弹窗只显示方向、UTC Bar 时间、AU2610 一行；没有 Event 身份、触发依据或更多定位/核对信息。

SubingDetailWorkspace.vue:113–114 模板与现场一致。列表中的公式和通知尝试信息甚至比该“详情”更多。属于复盘能力不完整，不代表持久 Event 丢失。

### 15 · P3 · HTDY 的“历史记录”重复展示数据区

黄金 60m 和焦煤 5m 的“历史记录”页先展示行情扩展、主力身份、数据可信，再显示历史。切回“数据与历史”主要只是去掉后面的历史列表。

页签职责重叠，找预警记录需要跨过不相关内容。焦煤历史列表确实有数据，不能误报历史不可用。

### 16 · P2 · STATUS 与实际版本不一致

develop 的 STATUS 仍称正式 Runtime 为 v1.10.9；实际 API 为 1.10.10。exact v1.10.10 Runtime 目录中的 STATUS 也仍写 v1.10.10 候选未发布、正式仍为 v1.10.9。

会影响后续诊断和交付状态判断。只确认文档漂移，不据此推断发布未经授权，也不建议修改 immutable tag。应在当前事实源补齐已批准交付的准确记录。

### 17 · P2 · 补充接口边界：大写 AU/JM 导致 500

本轮只读补测 AU/JM × trend/oscillation/main_rise，共 6 个 chart 请求，全部返回 500、`NEWOW_INTERNAL_ERROR`，耗时约 0.002–0.003 秒。

正常 Web 使用小写，因此这不是本轮正常点击产生的 500。即使接口合同只接受小写，也应输入校验并明确拒绝，而非作为内部错误。与焦煤小写请求的真实数据 409 分开处理。

### 18 · P2 · 默认周线统计窗口的 partial 状态缺少直接解释和恢复引导

黄金三策略主图截至完整周 9/11，但默认统计终点为 9/14，参考区提示 `warming（NEWOW_REFERENCE_WEEKLY_WINDOW_PARTIAL）`，顶部“当前参考交易/浮动”显示当前窗口不可用，下面却有已验证历史记录和统计。

黄金震荡点击“最近可用历史快照”后显示快照截至 9/14 15:00，图表仍截至 9/11、参考仍 warming。接口的历史快照合同只验证 chart 和 zhaoyao_mirror，因此不能推断它承诺修复参考窗口；**问题是用户难理解为什么有历史记录而顶部不可用、也缺少针对参考统计的明确操作引导**。

不把“本周未完成”本身报成行情错误，不要求伪造本周 Bar，也不把有意的 fail-closed 改成 ready。需要解释截止时间、哪些记录仍有效、用户如何选择完整统计区间。手动 9/11 的最终结果本轮未验证，保留边界。

### 19 · P3 · “指标解读”主要返回加载状态，解释信息不足

黄金趋势 → MACD → 指标解读。弹窗只显示 MACD、已加载/仅展示服务端事实、`ready · —` 和来源折叠入口，没有 DIF/DEA 的当前值、对应时间或用户可读的指标含义。

该功能名让人预期看到能辅助人工复核的解释，实际只给加载确认。属于产品完整性问题；不应通过编造指标结论或新增未验证的策略 Gate 来填充。

## 正常限制和已恢复项

- Newow 仅 W1 开放，D1、60m、跨周期综合解释未开放；杯柄仅适用 1d、主升浪比较器 not applicable，均是明确能力限制，不额外计 bug。
- 完整周线截至 9/11，在周一 9/14 属于正常 completed-only 行为。
- 黄金三策略主图和苏冰历史参考均已恢复；不能沿用 17:39 的全部不可用结论。
- 未发现把零费用页面参考直接写成真实账户成交的现象；换月中断、策略持有和参考交易状态保持区分。
- 消息“已尝试发送”不代表实际收件；本轮没有真实发送、重试或创建账户操作。
- 当前收盘、Live 不订阅可以正常；本轮没有自然夜盘、后续盘后或首次周检验收。

## 证据、验证和交付

本轮 Chrome AX 读回与截图记录在本会话工具输出，覆盖首页失败、黄金图表及长数字、趋势全屏裁切、苏冰参考/详情、焦煤三策略失败、消息筛选和跳转。截图没有单独导出为文件，不提供虚构截图链接。

本轮新增：

- 本报告。
- `fresh-browser-support-1900.json`：10 次顺序只读 GET，3 次 200、1 次 409、6 次 500；每项保存实际时间、耗时及响应。文件名表示本轮开始时段，精确时间以 checked_at 为准。

较早证据 `recheck-current.json`、`recheck-six-combinations.json` 只用于标注诊断上下文与状态变化，不当成本轮重新请求。

实际执行：git status/branch/rev-parse/worktree list、curl /health、Python urllib 顺序只读 GET、现役 tag 与相关源码读取、Chrome 真正点击/AX/截图。未修改业务源码，没有运行单元测试或 fixture E2E 冒充真实验收；只对报告和证据做结构/引用/格式/敏感字段检查及 git diff --check。

无业务代码提交、develop 集成、生产数据/配置/Scope 修改、通知发送或 Runtime 切换。无生产回滚事项。本轮仅改变临时页面状态；EMA10/21/60、Range 已恢复关闭，消息日期恢复 9/8–9/14，退出全屏和弹窗。

唯一最小下一步：**先对首页 49.857 秒请求及目录依赖做只读分层耗时定位，收敛一份精确修复范围。** 焦煤缺口须独立按数据事实核验后再决定写入 Gate，不能为页面验收直接补数。

最终结论：**阻塞**（主要页面仍未通过产品验收；本轮巡检与问题整理已交付）。
