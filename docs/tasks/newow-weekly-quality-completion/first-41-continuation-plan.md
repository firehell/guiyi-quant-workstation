# 首批 41 品种周线接续执行计划

日期：2026-09-18。Owner 已要求创建新的 Terra medium 任务执行余下工作。
本文件补充 [详细执行计划](first-41-execution-plan.md)，业务设计仍按 [闭环方案](first-41-closed-loop-plan.md)。
使用 writing-plans / executing-plans 工作方式：先核查真实基线，按阶段实现、验证、修复并连续推进。

## 1. 交接事实与明确范围

- 已完成提交：`b107f323c6ab819db94d82cd58c40b94a787dfa6`，分支 `codex/newow-first-41-weekly`。
- 原执行树：`/Users/zhangzhao/.codex/worktrees/186a/guiyi-quant-workstation`，本次核查干净。
- 已改 capability v4、weekly_products、API product-frequency Gate、类型/spec 和局部测试。
- 父基线 `11a159840` 包含完整执行设计；D1 快照/报价修复 `d7e4d5a76`、`a223ece1f` 已包含。
- 主任务本轮复验 API、candidate preview、readiness 三文件测试：91 passed / 2.16s。
  这只证明该组测试，不证明周线质量、页面、维护、独立 Review 或可发布。
- 原执行索引只记录任务0/1进行中，任务2–6未开始；没有任一品种的新 READY/页面验收结论。
- `useNewowCapabilities.ts` 仍仅按 open_frequencies 判开放，没有按当前 product 使用 weekly_products。
- reader quality_prefix 和默认 snapshot 仍限定 DAILY。`b107f323c` 不能独立作为周线发布候选。
- 此刻源 develop 仍为 `11a159840`；已经出现 v1.10.14 Runtime 工作树，不能继续依据旧 STATUS 或目录名推断运行事实。

固定分母41品种/123策略组合，执行批次不变：

```text
6：AU RB CU BU PD PT
17：A AG AL AO AP C CF EC FG FU HC I JD JM L LC LH
18：M MA NI P PB PP PS RM RU SA SC SN SS TA UR V Y ZN
```

## 2. 新任务启动与继承

- [ ] 使用宿主新建的独立 worktree，不嵌套建树，不在原执行树/源 develop/Runtime 修改。
- [ ] 在自己的 `codex/` 任务分支核对并吸收 b107f323c 的已有工作和本计划提交，再核对最新 develop 依赖。
  已是祖先则不重复 cherry-pick；分叉先审 diff 再常规合并，不 reset、force 或盲目覆盖。
- [ ] 若新树默认从 main 开始、计划文件尚不存在，先读源仓库绝对路径下本文件及两份原计划，再吸收本地提交。
- [ ] 记录真实 HEAD、导入目录、工作树、scope/config/Canonical 非秘密身份及当前维护活动。
- [ ] 复用主工作区已安装 Python/Node 时明确 PYTHONPATH 指向新树，避免实际运行旧代码。
- [ ] 读取原索引作为历史进度，本轮建立一个可恢复证据索引；保留旧测试记录，不复制为本轮新验收。
- [ ] 回读 D1/AU smoke 及41实际消费依赖，原始窗口不缩；有缺口生成差量，不删除失败品种。

## 3. 剩余工作的依赖顺序与验收出口

### R1：补完能力范围和时间合同（原任务0/1收尾）

- [x] `useNewowCapabilities.ts` 增加 product-aware 判断，相关页面传入当前 product；切换品种立即重算。
- [ ] 前端从已验证 capabilities 消费范围，不把前后端重复字面清单当作两个权威；严格校验合法类型、唯一性与范围语义。
- [ ] 所有页面/深链接/显式 API/历史入口/比较器的41资格一致，19品种明确待开放，D1全60保留。
- [ ] 核对 readiness 的只读全域审计与产品发布 scope：可审计未开放品种输入，但不得把它记作可发布 READY。
- [ ] 同步 expected/available周端点、snapshot_as_of、current_context、更新/过期状态的 schema 与 canonical。
- [ ] 增加切换 AU→未开放品种→AU、旧偏好/链接、scope版本、D1兼容回归。

出口：范围合同上下游一致；进入 R2，不在此提交后结束任务。

### R2：周线质量与参考交易（原任务2）

- [x] 核实旧周质量实现是否已在其他提交中完成，能证明符合合同才复用，不整分支复制。
- [x] 完整ISO周、同物理合约来源、预期D1端点互斥覆盖、合法短owner、不完整来源和revision冲突分别测试。
- [x] 把已证实缺价周的无OHLC中断证明贯通MDS→reader→calculation segment→策略→ReferenceTrade→缓存。
- [ ] 周中断只在完成端点生效；断后有效周重新预热，NO_TRADE不等同中断，未知缺日不获豁免。
- [x] 跨断点/合约不配对，不造BUILD/exit/收益；D1身份和原公式不变。
- [ ] 测试prefix/batch/incremental/restart一致性；来源冲突保留真实阻塞，继续其他独立工程。

出口：原详细计划任务2的质量与重放测试通过；进入 R3。

### R3：完整周默认快照（原任务3）

- [ ] 服务端统一周截止，三策略/各面板共用该事实；独立当前报价、owner、换月状态。
- [ ] Calendar/Session决定端点，禁止硬编码周五/历史日期；不以策略READY或收益选择周。
- [ ] 只有可证明的末尾发布等待才可使用紧邻前一完整周；历史内部缺口/错身份/损坏不得后退掩盖。
- [ ] 新周缺价/预热不足展示新周状态，不回旧READY；维护失败/过期不能显示普通pending。
- [ ] 测试周内当天Map缺失、周五发布前后、下周首次打开、长假短周、跨月/跨年、夜盘、周中换月。
- [ ] 实现有界超时/取消和依赖版本缓存；显式历史与默认解析分离，保留D1接口/行为回归。

出口：默认周快照合同与反例通过；进入 R4。

### R4：页面垂直贯通与41矩阵（原任务4/5）

- [ ] 页面先取得快照再加载各面板；历史参考持有带截止，当前未知不显示当前持有。
- [ ] 切换品种/周期/策略时清理旧token/分页并取消请求，旧响应不可污染新身份。
- [ ] 核对隔离 preview 实际支持墙钟默认解析；必要的只读扩展不能绕过身份校验或启动正式writer。
- [ ] 先6品种18组合完整链路，再17、18批次；逐品种记录数据/来源/策略/页面状态和实际证据。
- [ ] 123组合覆盖主图、全部开放辅助、参考/收益及比较器；真实首载检查主图、默认副图、参考与时间标签。
- [ ] D1全60×3回归；19周线、60m、完整explanation保持未开放。
- [ ] 真实矩阵预算不足保留未检项，按测量安排剩余只读部分，不缩窗/分母，不靠预热刷新补绿。

出口：候选123组合都有可信结论；部署后公网验证仍保留外部Gate。进入 R5。

### R5：持续维护和必要数据差量（原任务6/7）

- [ ] 盘后终态与锁释放后运行有界 newow_w1 消费检查，数据维护成功与页面验证分开。
- [ ] D1/W1共享可测资源边界；锁忙、超时、输入变更不计通过，不触发额外下载/重试/通知。
- [ ] 验证新owner最小消费前缀，复用planner形成必要D1+W1补齐路径；不能在GET/只读审计里写数据。
- [ ] 若产生真实数据差量或持续维护扩围，先准备目标、环境、精确窗口/hash、预算、幂等和恢复包，再请求授权。
- [ ] 未授权部分停在prepare；继续不依赖写入的工程、测试及Review。获批后串行执行、独立读回。

出口：维护工程和数据/持续授权差量清楚；进入 R6，不以“需要未来授权”代替现有工程。

### R6：独立Review与工程收口（原任务8）

- [ ] 完成定向/相关模块测试、Web test/build、OpenSpec、secret scan和diff检查；使用原计划具体命令。
- [ ] 固定真实base/head做独立Review；优先时间退让、scope、质量、配对、缓存、权限和D1回归。
- [ ] 修正Confirmed Issue、补测；重要风险明确是否阻断。单次91passed不覆盖本阶段新变更。
- [ ] 仅提交本任务文件，按当前规则完成可执行的develop集成和exact candidate复验。
- [ ] 准备发布与Runtime操作包，说明数据schema兼容及恢复；没有批准不发布或切换。

出口：所有独立工程真正完成；缺外部操作时明确 CODE_COMPLETE_EXTERNAL_GATE_PENDING，
若仍有工程/数据/验收阻塞则报 PARTIAL，不能误报代码完成。

### R7：获批后的部署和自然接续（原任务8/9后半）

- [ ] 先核对已有授权是否明确覆盖本版本和范围；没有则请求精确发布/Runtime批准。
- [ ] 部署后本机与公网各123 W1默认首载、D1回归，读回实际服务代码/数据身份。
- [ ] 使用自然完整周维护与随后默认使用证据；未发生的新周/换月单列待验，不手动造事件或新建自动化。
- [ ] 最终分别给出41品种数据/策略/页面/发布/Runtime/自然接续结论；STATUS只更新真实已通过项。

## 4. 连续推进规则与真正停止条件

本轮不是“只做下一个提交”。每阶段完成就自动推进下一阶段，阶段进度用 commentary，不把普通阶段结束当作最终交付。
不要因计划条目多、任务较长或普通测试失败提前结束；使用复选框和证据索引跨上下文恢复。
可以结束本轮执行的情况只有：目标全部完成；或者尚未满足的外部授权/真实语义决定/宿主权限使剩余工作确实无法继续，
并且已经完成所有不依赖该阻塞的已授权工作。需要用户时说明精确阻塞、原因、现成操作包和最小决定。

真实生产下载/写入、持续维护扩围、main/tag/Release、Runtime和通知遵守AGENTS.md；本轮用户要求执行不隐含上述授权。
此前文档 push 曾被自动审批以远端归属未证实拒绝；owner 已于 2026-09-23 确认本仓库 origin 及常规文档/`develop` push 授权，后续按 `AGENTS.md` 执行。
只有远程地址、仓库归属、推送目标发生变化，或向其他外部目的地发布时，才需另行确认；本地继承提交不依赖推送。
普通测试/权限失败如有合规替代可继续，不修改全局配置、不读取凭据、不终止其他任务或生产服务。

最终回复必须同时列：新增提交、实际测试结果、41/123矩阵分母、独立Review、集成状态、外部Gate、下一步。
保留“工程可交付”与“部署后自然闭环通过”的区别。
