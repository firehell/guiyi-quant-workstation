# 牛哇 W1 正式页面验收（2026-09-26）

## 范围与版本

本次独立验证 W1，不以 D1 历史保留结果代替。API 冻结 `2bfc4a6a8`，页面基于
`6ebcba6e5` 加修订 `f8c1353e4`。使用正式 API 路由与真实 Canonical/Catalog，只读交易；
仅在任务端口 8011/5175、8010/5174 启动隔离进程，没有数据写入或来源下载。
现役 5173/8000 仍为 v1.10.34（`85d235206cd4a41602c68a637a9d563c60941ece`）。
本次是冻结候选正式路由页面验收，不能宣称该修复已经在现役 Runtime 生效。

## 发现与修复

真实 AO 周线 MACD 当前区段可计算，历史短区段为 WARMING；完整有值响应因为仅 READY
进入快照缓存而丢失 token，前端严格对齐因此隐藏。修复让 delivered、有值且
READY/WARMING 的结果进入既有 proof/token/原子容量校验，不改变计算公式、门槛、coverage
或 WARMING 状态；没有放宽前端 token 校验。部分参考覆盖仍为 WARMING，不变成完整覆盖。
另将失败页的“刷新日线”改为“刷新当前”，适用于实际选定的周线。

## 页面证据

证据根目录：`outputs/newow-w1-page-acceptance-20260926/`。
`http-journal.jsonl` 保存请求参数、响应文件与 SHA256；`surface-alignment.json` 检查图表、
辅助、参考响应的同 token、同 as_of。原始失败记录保留，不计入最终通过证据。

- AO 趋势：MACD“当前可计算；历史部分仍在预热（5 个区段）”；照妖镜为 2 个历史预热区段。
- PL 趋势：主图与参考可读，MACD/照妖镜当前不足，分别 5/12 个历史预热区段，未强行 READY。
- RS 震荡：RS2701 仅两根已完成周线；主图/辅助/参考保持预热，显示“当前数据不足，待每日增量积累”，历史图形与验证记录保留。
- JM 趋势→主升浪→震荡页面切换分别重新绑定本策略 token，三层共用同一截点，不混用上一策略结果。
- JM：MACD、主力控盘、涨跌动能当前可计算；趋势转折当前段 35 根未满 120 根，明确预热。
- 正常页面截止为 `2026-09-24T07:00:00.000001Z`；bar 时间 07:00:00Z 在该请求截点之前，符合 completed-only。

### 周线更新未到与失败（隔离可控场景）

只对进程内 JM 主力映射读取视图暂时排除 9/21–9/24，固定请求时点 9/24 08:00Z；
真实旧行情不变。用既有 publication_state 合同分别模拟 pending 与 failed，不改生产配置或数据库。
这是可控故障验收，不是自然盘后更新成功证据。

1. pending 冷启动：图表/辅助/参考均显示 `9/18 07:00:00.000001Z`，明确 `9/24` 周线待发布，报价不可用。
2. 改为 failed 后刷新：既有 9/18 三层结果仍可见，出现 `NEWOW_WEEKLY_FAILED` 警示。
3. failed 冷启动：明确主图事实不可用，不自动伪造当前快照；点击“查看最近可用历史快照”后恢复真实 9/18 三层，标为历史窗口，隐藏当前报价。
4. normal 页面恢复真实 9/24 截点，不把 9/18 历史值当成当前报价。

“统一参考交易”持久化面板返回既有 503，显示“已保存的历史参考暂不可用”；
它是独立 P9 reader，不是本次 Newow 策略页面参考投影。该面板未验收通过，不以页面投影通过掩盖。

## 实际验证

- `60-weekly-final-snapshots.json`：60/60 正式 W1 默认快照通过，均截至 9/24，freshness=current；代码 `2bfc4a6a8`。
- pytest `test_product_service.py test_product_snapshot_cache.py test_weekly_snapshot.py`：81 passed。
- Node 四组面板/图层/加载器回归：126 passed；正式路由/类型两组：47 passed。
- vue-tsc、Vite build、生产入口拓扑：通过；既有动态导入打包提示不影响通过。
- 独立 Review：65 passed，无 Confirmed Issue；proof 冲突与容量拒绝保护保留。
- `git diff --check` 通过。

## 结论边界

页面投影与周线历史保留使用本次独立 W1 证据。自然更新与现役 Runtime 上线仍是后续步骤；
本次不发布 main/tag，不切换现役副本。完整只读回归不替代自然业务运行证据。
