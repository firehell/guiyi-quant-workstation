# 首页慢请求与目录依赖修复

日期：2026-09-14。状态：CODE_COMPLETE / TEST_COMPLETE / REVIEW_COMPLETE。

修复提交：`54004dea247c90a0238b19f4dab41b2e6432160c`，基线：`4c891d8df24c4a5206cfcad50712012e3c564fcf`。
已快进合入并推送 develop，远端 SHA 已读回一致；本次临时工作树与已合并任务分支已清理。
开发集成不代表发布或 Runtime 切换；本次未修改正式服务、行情资产、数据库事实、公式或投影开关。

## 已完成修改

1. `services/quant-api/app/market_data/market_data_service.py`：同一次周线分页读取中，候选过滤和分页边界验证共用同品种、同 ISO 周的交易日历。缓存随请求结束；下一请求仍读取更新后的日历。主力映射、完整周、物理完整性、错误和游标语义保留，Catalog 公共接口未修改。
2. `apps/quant-web/src/composables/useMarketHome.ts`、首页和 Header/Messages：用现有批量 `/market/dominants` 独立提供搜索、导航和消息筛选目录；复用既有品种规范化函数。不再从 overview 行情参与行构造目录。目录独立缓存、去重、失败提示，刷新失败保留上次可用选项。
3. 首页原有可见页 60 秒定时器检查 overview/目录的五分钟缓存和失败状态；成功未过期时不重读，失败时可恢复。保留并发请求合并、generation 隔离及隐藏页停止定时器。
4. 对应两份 OpenSpec、DECISIONS 和 TESTING 已同步。首页批量目录请求加入 E2E 白名单，仍禁止每品种扇出和写请求。

## 真实性能与一致性

使用候选实际代码及真实 Catalog/Canonical，在同一 repeatable-read/read-only 事务中比较基线与修复；不是模拟数据，也不是生产 HTTP 部署验收。

| 样本 | 耗时 | SQL 次数 | 品种数 |
|---|---:|---:|---:|
| 本轮基线 | 50.198 秒 | 57,412 | 60 |
| 同事务修复版本 | 11.234 秒 | 11,142 | 60 |
| 独立进程修复复测 | 12.235 秒 | 11,142 | 60 |

同事务 SQL 减少约 80.6%，耗时降低约 77.6%。三份完整响应 SHA-256 一致：
`3eaba246ac96f1e79831d67c0dd3b7cd31ed909f2bd0e887dba6e400ae0a2c73`。

证据：`implemented-read.json`、`implemented-read-isolated.json`。诊断脚本为 `verify_implemented_read.py` 和 `verify_implemented_isolated.py`，仅在独立进程装配版本对照，数据库事务只读并 rollback。

此前 49.857 秒 HTTP 无同期 profiler，本轮 50.198 秒是独立读取基线，不能称为原 HTTP 请求的精确重放。
修复后仍为约 11–12 秒，尚未达到定位报告提出的低于 10 秒初始目标，也不证明投影命中低于 200 毫秒。
当前结论是已去除主要重复查询成本并保持响应一致；正式 HTTP、并发负载及部署后的体验仍需候选版本/Runtime 的独立验收。

## 实际验证

- 新增回归先失败后实现：同周日历重复读取（原为 2/6 次）、持续可见页面的失败恢复（原未重试）、行情失败时黄金目录不可用、独立目录请求（原为零次）。
- 后端定向组：分页、首页计算、投影及 API/失效/盘后投影边界，共 **98 passed**。
- 共享读取扩展组：Catalog/MDS、MarketRead、Market API/research、actual-dominant research、首页 composition、分区边界，共 **211 passed**。
- Web 完整单元：**602 passed / 1 既有 skip**（603 项）；日志 `web-unit.log`。
- 首页真实 Chrome fixture E2E：**29 passed**；日志 `home-e2e.log`。包括 60 行及多分辨率原有行为、overview 加载中/失败/有效零行时 AU/JM 搜索和消息筛选、目录初次失败与恢复、后续失败保留选项。
- Web production build（含 vue-tsc 与 bundle topology）通过；日志 `web-build.log`。存在既有 dynamic/static import 提示，无构建失败。
- Mypy：目标源文件无问题；Ruff：通过；工程 canonical consistency：**17 passed**；OpenSpec：**9/9**；secret scan：**0 finding**；`git diff --check`：通过。

命令入口已写入 `TESTING.md` 的“首页慢请求、目录独立与恢复回归”。实际运行使用现有 Python venv 的 `python -m pytest -q -p no:cacheprovider`、`python -m mypy --explicit-package-bases --ignore-missing-imports`、`python -m ruff check`，以及 `pnpm -C apps/quant-web test/build`、`pnpm -C apps/quant-web exec playwright test -c playwright.config.mjs e2e/market-home.spec.mjs`。

## Review 与交付边界

独立 Review 首轮发现零行 E2E 的 `data_as_of=null` 使 fixture 被规范化器拒绝。已改为合法日期，增加成功零行及无首载失败断言，并通过真实规范化和浏览器复验。新增恢复测试在点击刷新后重新聚焦搜索框，与组件关闭弹层行为一致。最终 Review 为 **0 finding，允许集成 develop**。

现役 Runtime 未切换，未执行 main/tag/release；这些 Gate 仍独立。未覆盖或清理原有巡检、数据恢复输出。
业务数据无回滚需求；代码需要回退时按普通 Git revert 流程处理该修复提交，不重写历史。

结论：**允许集成 develop**。唯一最小下一步：在独立发布 Gate 中将该提交纳入候选版本，验证真实 HTTP 与 AU/JM 页面；不把本次代码集成视为已上线。
