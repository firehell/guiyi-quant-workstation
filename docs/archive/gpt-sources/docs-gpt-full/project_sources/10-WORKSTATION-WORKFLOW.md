# 10 Workstation Workflow Compat Summary

状态：`compat_summary`

Canonical source：`docs/workstation/`; `docs/workflows/`

本文件仅用于旧的 `docs/gpt/project_sources/` 上传流程兼容，不维护第二份工作站流程事实。若本文与 canonical 文件冲突，以 `docs/workstation/` 和 `docs/workflows/` 为准。

读取建议：

- GitHub Native 控制平面、Issue-first、TASK、Draft PR、dispatcher、writer lock 和 Gate，请读取 `docs/workstation/`。
- 工作级别和交付流程请读取 `docs/workflows/`。

