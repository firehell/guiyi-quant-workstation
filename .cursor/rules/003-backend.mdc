---
description: 后端开发规范 — Python FastAPI
globs: ["services/**/*.py", "packages/**/*.py"]
alwaysApply: false
---

# 后端开发规范

## 技术栈

- **框架**：FastAPI + Uvicorn
- **ORM**：SQLAlchemy 2.0（异步模式）
- **数据库**：PostgreSQL 16 + Redis 7
- **数据处理**：pandas + polars + numpy
- **序列化**：Pydantic v2
- **任务队列**：Redis + RQ
- **测试**：pytest + pytest-asyncio

## 代码规范

```python
# ✅ 正确：类型注解 + docstring + 异步
from decimal import Decimal
from typing import Optional
import pandas as pd

async def get_strategy_metrics(
    strategy_id: str,
    start_date: str,
    end_date: Optional[str] = None
) -> dict[str, Decimal]:
    """
    获取策略绩效指标。

    Args:
        strategy_id: 策略唯一标识
        start_date: 统计开始日期，格式 YYYY-MM-DD
        end_date: 统计结束日期，默认为今日

    Returns:
        包含 sharpe、max_drawdown、annual_return 等指标的字典

    Raises:
        StrategyNotFoundError: 策略不存在时
    """
    ...
```

## API 路由规范

```
/api/v1/strategies/          策略管理
/api/v1/backtests/           回测管理
/api/v1/market/              行情数据
/api/v1/positions/           持仓管理
/api/v1/risk/                风控接口
/ws/market                   行情 WebSocket
/ws/positions                持仓 WebSocket
```

## 数据存储约定

- 原始行情：`data/raw/` + parquet 格式
- 处理后数据：`data/processed/` + parquet 格式
- 缓存热数据：Redis（TTL ≤ 60s）
- 持久化业务数据：PostgreSQL

## 安全要求

- 所有外部输入必须通过 Pydantic 验证
- SQL 查询统一通过 ORM，禁止字符串拼接
- 敏感接口必须 JWT 鉴权
- 生产环境禁用 `/docs`（OpenAPI）
