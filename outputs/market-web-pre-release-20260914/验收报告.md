# Market Web 发布前十一项验收报告

日期：2026-09-14（Asia/Shanghai）

候选：`codex/market-web-pre-release`，实现提交 `11bd54017..1a936c416`

范围：只读代码、测试、浏览器与现役状态复核。未执行 RQData 下载、Canonical/数据库/Redis 写入、
Scope/Rule/通知变更、main/tag/Release 或 Runtime switch。

## 结论

状态为 `CODE_COMPLETE / TEST_COMPLETE / REVIEW_COMPLETE / EXTERNAL_GATE_PENDING`。代码与展示层验收通过；
JM 的真实物理合约历史缺口没有被页面降级、fixture 或合约替换掩盖，仍须独立数据修复 Gate。候选不属于
当前 `v1.10.10` Release/Runtime。

## 十一项验收矩阵

| # | 验收项 | 代码/fixture 证据 | 真实只读 API/Chrome 证据 | 结论 |
|---:|---|---|---|---|
| 1 | JM SuBing 不再只显示不可判定 | 后端返回 allowlist 的 stage/reason/context；页面显示公开诊断 | JM 返回 409：`physical_contract_replay / DATASET_OR_PARTITION_MISSING`，合约 JM2609，expected_count=5081 | 诊断闭环通过；数据 Gate pending |
| 2 | AU/AG/JM 大小写一致 | API 在 cache/service 前统一归一为大写；六组参数测试 | 最终候选对 AU/au、AG/ag、JM/jm 做成对 HTTP 读回 | 通过 |
| 3 | 首页低于 10 秒 | 60 行、显式重载与错误面回归 | 60/60；Chrome 冷加载 8526ms、重载 7863ms；API 稳态约 7.5s | 通过 |
| 4 | 品种选择器搜索/键盘/焦点稳定 | unit + fixture E2E 覆盖程序化 focus 一次抑制、键盘选择和不重开 | 最终候选 Chrome 搜索 JM 并进入详情，未出现焦点抖动或下拉重开 | 通过 |
| 5 | SuBing 四类事实不混淆 | Scope、Rule/Runtime、全局评价、当前品种 stored Event 分开；无 Event 不解释为中性 | 候选仅开放历史 reference；正式 health/Event 另行只读读回，未伪装成候选快照 | 通过 |
| 6 | Decimal 显示无 Number 精度损失 | 字符串算法覆盖指数、超大值、极小值、半入舍入和负零 | AU/JM 页面数值渲染与 raw 属性保留 | 通过 |
| 7 | 时间与术语统一 | 可见 Event/history/explanation 时间均转北京时间；reason/动作中文化 | AU/JM 候选页面未暴露内部 reason code，时间按北京时间展示 | 通过 |
| 8 | 移除无依据 5 日涨跌 | 详情页不再渲染静态 `—` 指标；单测锁定 | 真实详情页无该伪指标 | 通过 |
| 9 | W1 未完成区间可显式切换 | 周一、节假日、跨年测试；只使用服务端边界 | AU 点击“使用最近完整统计区间”后切至 2026-09-11，并重新请求 | 通过 |
| 10 | STATUS/Canonical/Testing 同步 | 本报告、STATUS、两份 OpenSpec、ARCHITECTURE、TESTING 同步 | v1.10.10 tag/main/Runtime identity 与 22:15 服务状态分别读回 | 通过 |
| 11 | 密集信号标记避让 | 纯布局算法覆盖 selected priority、稳定性、碰撞、边界、缩放与 raw id | AU trend 加载 32 个真实 callout；1440、390 与全屏边界均通过 | 通过 |

## 真实数据边界

固定 `as_of=2026-09-14T11:00:00Z` 的只读复核中：

- AU 使用 `AU2610`，实际主力重放 1295 个端点，物理合约重放 expected/actual 均为 8785；SuBing 参考为
  2 笔 reference、37 个 signals。
- JM actual-dominant 重放为 2353 个端点；`JM2609` 在 2025-09-15 至 2026-08-18 物理窗口缺失。
- JM 缺口覆盖 223 个预期交易日中的 139 日，缺 3165 个端点；连续范围为
  2025-09-15..2025-09-30、2025-10-09..2025-12-31、2026-01-05..2026-02-13、
  2026-02-24..2026-04-16。
- 这与既有 Newow JM W1 历史缺口是两条独立证据，不得互相替代。

## 验证摘要

- 后端扩展定向组：222 passed；候选隔离组：28 passed；Ruff、Mypy 通过。
- Web unit：614 tests，613 passed、1 个既有可选 golden skip；typecheck、production build 通过。
- fixture 浏览器矩阵：最终完整组通过。
- 真实候选浏览器：60 品种首页、AU/JM Newow/SuBing、最近完整周、callout 桌面/移动/全屏通过。
- 同会话 Review 分 Standards 与 Spec 两遍检查，无剩余可行动 finding。

## Release/Runtime 分界

当前正式身份为 `v1.10.10@b49e2499de654092b48e60e181102c02e16ce89f`。API/Web、Live/Alert 与当日
自然盘后已只读通过；provider accepted 不证明用户实际收件，weekly audit 尚未自然运行，因此不声明
`RUNTIME_READY`。本候选未进入 main/tag/Release/Runtime。

## 唯一最小下一步

为上述 JM2609 15m 精确缺口生成一份只读、带 hash 的 repair plan 与配额估算，交 owner 单独批准数据 apply；
在批准前保持零 provider 请求与零正式写入。
