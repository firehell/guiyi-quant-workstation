# V1.5 验收标准与测试清单

> 配套分支：`feature/unified-task-dispatcher`
> 调度器：`scripts/ai/dispatch_task.sh`
> 自检：`scripts/ai/workstation_doctor.sh` + `make workstation-test`

---

## 验收标准

| AC# | 验收标准 | 验证方式 |
|-----|---------|---------|
| AC1 | 统一 stage 入口 `route\|plan\|dev\|fix\|test\|review\|result` | `test_task_router.py`、A–I 集成测试 |
| AC2 | 四档路由 tier（fast/standard/deep/critical） | `test_routing_scenarios.py` A–D |
| AC3 | Writer lock 与生产/环境 fail-closed Gate | E–I 场景 + `test_task_router.py` |
| AC4 | `pause\|resume\|cancel\|status` 控制阶段 | `test_pause_resume_cancel.py` |
| AC5 | `CANCELLED`/`PAUSED` 静态 Gate | cancel/resume 集成测试 |
| AC6 | Issue 外部操作默认 dry-run（exit 6） | `test_issue_dry_run.py` |
| AC7 | `approve_task.sh --confirm-production-write` | `test_issue_dry_run.py` |
| AC8 | doctor F02 状态/产物一致性扫描 | `workstation_doctor.sh` f02 check |
| AC9 | CI `workstation-test` workflow | `.github/workflows/workstation-test.yml` |
| AC10 | 文档与实现一致（ROUTING_POLICY、fault_handling、AGENTS） | 人工签字 |

---

## 场景映射（A–I + 控制面）

| 场景 | Fixture / 测试 | 说明 |
|------|---------------|------|
| A fast doc | FAST_DOC | fast tier + workspace-write dev |
| B standard API | STANDARD_API | standard tier |
| C deep runtime | DEEP_RUNTIME | deep 自动升档 |
| D critical indicator | CRITICAL_INDICATOR | external_review_required |
| E blocked production | BLOCKED_PRODUCTION | 生产 Gate |
| F wrong branch | WRONG_BRANCH | Branch Gate |
| G locked worktree | LOCKED_WORKTREE | writer lock 冲突 |
| H missing mount | MISSING_MOUNT | 环境 fail-closed |
| I forbidden path | FORBIDDEN_PATH | collect 阻断 |
| Pause/Resume/Cancel | LOCKED_WORKTREE | T20–T22 对齐 |
| Issue dry-run | ISSUE-OPS fixture | AC6 |

---

## 人工 smoke（021 TASK，不耗 Codex 额度）

```bash
make workstation-test
bash scripts/ai/dispatch_task.sh TASK-2026-07-12-021-workstation-v1.5-pause-cancel route --dry-run
bash scripts/ai/dispatch_task.sh TASK-2026-07-12-021-workstation-v1.5-pause-cancel plan --dry-run
bash scripts/ai/dispatch_task.sh TASK-2026-07-12-021-workstation-v1.5-pause-cancel status
```

签字项：上述命令 exit 0；`status` 输出含 `approval_present` / `stage_logs`。

---

## 100% 完成判定

- [x] TASK-020/021/022/023 `DELIVERY_READY`
- [x] `make workstation-test` 在 feature 分支通过（50 pytest + doctor 14 passed）
- [x] V1.5 AC1–AC9 有自动化测试或 doctor 检查
- [x] pause/resume/cancel/status 与故障手册 F06/F10 一致
- [x] Issue 脚本默认 `--confirm-issue-ops` Gate
- [x] CI workflow 存在
- [x] `tasks/current.md` / `CODEX_HANDOFF` / `gpt/*` 已同步
- [ ] 合并 `main`（人工 Gate，见 TASK-023 §21）
